# ODD task: Issue #181 typed TypeScript modules

## Status and evidence boundary

- Ticket: [#181](https://github.com/eff3ct0/agent-foundry/issues/181), open with `type:product` and `status:approved` at planning readback.
- Phase: `EVIDENCE/DELIVERY` checkpoint for the bounded TM-01a foundation; the full TM-01 runtime decision and remaining migration are open. No bulk rename or module-policy claim in this slice.
- Worktree: `/home/steam/git/agent-foundry-worktrees/issue-181`; branch: `feat/issue-181-typed-modules`; starting `HEAD`: `d6b0f68a961ab4a312f2a2dbbae332a408ff5a5d`.
- Source of requirements: full issue #181 body and acceptance criteria read through read-only `gh issue view 181 --repo eff3ct0/agent-foundry` on this branch. The local self-governance text still names `eff3ct0/factory-template`; use the explicitly authorized issue URL for this plan, without silently changing unrelated repository coordinates.
- Authorization: the owner's original request expressly authorizes implementation and issue-specific work-unit PRs using the approved authenticated `gh` session for `eff3ct0/agent-foundry`. Earlier planning-only and local-implementation delegate limits described those earlier slices only. This passive documentation checkpoint authorizes one docs commit and an HTTPS push of `feat/issue-181-typed-modules` only; no other ref mutation, merge, issue comment, draft flip, hosted workflow dispatch, publication, or approval-gated action.

## Objective, problem, and boundaries

Make the archetype's own ESM implementation modules typed TypeScript `.mts`, eliminate tracked and archetype-generated `.mjs` modules, and prevent future untyped JavaScript implementation modules from entering the archetype. Today the extension and generated-asset boundaries are not enforced; a new `.mjs` file could bypass the typed-source convention, while an indiscriminate extension ban would incorrectly reject host-required `.ts` integrations or compiled output.

In scope: tracked-module and payload inventories; `.mjs` to `.mts` migration where applicable; import and runtime specifiers; compiler/build/test/CLI execution; ownership and payload lock; startup/hooks, workflow and CI consumers; documentation; deterministic path-reporting policy for both source and fresh generated support assets; focused and end-to-end offline regressions.

Out of scope: new runtime behavior, provider/CI recipe semantics, unrelated TypeScript refactors, rewriting historical task evidence, migrating an initialized user's application code, publication, hosted workflow dispatch, and unrelated remote mutations. The earlier first-child implementation slice was local-only; its PR delivery is recorded below. Keep existing `.ts` host integrations (`hooks/opencode/factory-start.ts`, `hooks/pi/factory-start.ts`, and the generated OpenCode plugin) where their host requires `.ts`. Existing creator `src/*.ts` is CommonJS under `package.json`/`tsconfig.json`; decide its treatment by actual module contract, not a blanket filename replacement.

## Baseline and constraints

- Tracked inventory at `HEAD`: 66 `.mjs` files (34 `scripts/`, 31 `test/`, and root `start.mjs`), totaling 7,834 source lines; seven `.ts` files; zero tracked `.js`, `.mts`, `.cjs`, or `.cts` files. This is a tracked-file inventory, **not** a passing policy check.
- The declared payload currently contains five `.mjs` paths: `start.mjs` and four inherited generic checkers/synchronizers (`scripts/check-delivery-contract.mjs`, `scripts/check-pr-governance.mjs`, `scripts/sync-github-labels.mjs`, `scripts/check-factory-layout.mjs`). Normal initialization relocates inherited generic scripts into `.factory/scripts/`; source-only release helpers and tests are removed.
- `package.json` uses CommonJS, `foundry` -> `dist/index.js`, `pnpm build` -> `node scripts/build.mjs`, and `pnpm test` -> `pnpm build && node --test test/*.test.mjs`. `tsconfig.json` includes only `src/**/*.ts` and emits CommonJS `.js`. `dist/` is ignored, yet the packed CLI includes compiled `.js`; an emitted file is not an untyped **source** module, but the policy must prove provenance rather than exempt arbitrary `.js`.
- TypeScript 5.8 `.mts` normally emits `.mjs`. Simply changing extensions and adding `.mts` to `tsconfig.json` would still produce generated `.mjs`, contrary to issue acceptance. Prove the chosen Node 20.19-compatible build/run approach and the typed provenance of any `.js`/`.cjs` output **before** a bulk rename. Do not rely on a TypeScript runtime loader or a bundler until validated offline.
- Payload inputs are controlled by `archetype-ownership.json` and `package/payload-files.json`; `scripts/build-payload.mjs` compares them with the checked-in `package/payload-manifest.json` and its digest. Rename/integrity changes must be synchronized as a single testable work unit.

## Acceptance criteria and verification plan

1. No tracked `.mjs` implementation/test/startup module, packaged `.mjs` module, or archetype-generated `.mjs` module remains. Source ESM TypeScript modules use `.mts`; generated code uses a documented, type-derived runnable format, without `.mjs` output. Keep host-required typed `.ts` files and resolve all imports and execution entrypoints.
2. A deterministic checker inventories the archetype-owned source and payload/generated support paths, rejects `.mjs` and untyped `.js` implementation modules, reports sorted offending paths with a nonzero exit code, and allows only demonstrably compiled typed JavaScript output and host-mandated `.ts` integrations. Do not police an initialized project's unrelated application-owned JavaScript or mistake data/fixture text for executable modules.
3. The checker has positive and negative fixtures (including source `.mjs`, untyped `.js`, nested support scripts, generated assets, host `.ts`, typed compiler output, and stable diagnostic ordering). It runs in normal source verification/CI and is executable against a fresh generated project.
4. Package entrypoint, startup routing, shell-free host hooks, source-only workflow helpers, static workflow assertions, packaged payload and manifest, local pack/consumer tests, and current supported provider behaviors still work. Documentation, workflow literals, ownership paths, and executable CLI help stay in sync.

**Existing applicable commands to run after implementation** (record exact exit/result, not just intent): `pnpm build`; `pnpm typecheck`; `pnpm test`; `pnpm test:package-consumer`; `pnpm test:workflow-contract`; `node scripts/typed-inherited-runtime/check-delivery-contract.js --self-check`; `node scripts/typed-inherited-runtime/check-delivery-contract.js --approval-self-check`; `node scripts/check-bootstrap-workflow.mjs`; `node scripts/typed-runtime/check-real-agent-workflow.js`; `node scripts/check-factory-layout.mjs`; `node scripts/check-determinism.mjs`; `node start.mjs --self-check`; `git diff --check`. Update the literal `node ...mjs` invocations here to their verified replacements as they migrate; do **not** run obsolete commands and report them as green. Node's built-in `node --test` is the current test runner, invoked by `pnpm test` after build; if compiled tests need a different path, prove it on Node 20.19 and update the package script and this record together. No lint/format script currently exists in `package.json`; record those gates as not configured, not as passing.

**New gate to implement:** provide stable `pnpm run check:module-policy` and `pnpm run test:module-policy` aliases, wire the source check into the normal `pnpm test`/CI path, and run the policy over a fresh offline `foundry apply` target as well as the repository and packed payload. These aliases are **planned, not present at this baseline**; settle the runnable checker filename and generated-project invocation only after the output strategy is proven. Exercise `foundry plan`, `apply`, `verify`, and rerun `noop` in a disposable target; run the generated startup and retained checker there, and check the complete generated-tree inventory. Use the existing offline package-consumer suite for packed-payload identity; no published-package or hosted journey claim is possible from a local check. Run the ownership/payload lock and workflow/static checks after every relevant boundary change.

## Tasks (IDs remain stable; no checkbox is complete by planning alone)

- [ ] **TM-01 — Resolve executable format and policy scope.** Route: direct technical spike / decision gate. Trigger evidence: the 66-file inventory, current Node 20.19/TypeScript 5.8 settings, `.mts` -> emitted `.mjs` conflict, and `dist/index.js` compiled CLI. Prove an offline runnable typed build/test/startup path with zero produced `.mjs` modules; document compiler output and emitted-JS classification before converting source. Check: isolated compiler/runtime proof, `pnpm typecheck` and representative Node execution; no rename on an unproven path.
- [x] **TM-01a — Isolated typed-emitter foundation (child #1 only).** `src/typed-module-emitter.ts` uses the existing TypeScript compiler API to typecheck `.mts` inputs, validate literal relative `.mjs` imports/re-exports/dynamic imports against local `.mts` files, rewrite emitted specifiers to `.js`, and capture compiler `.mjs` output in memory before writing ONLY `.js` into a fresh output directory with a nested `package.json` declaring ESM. It rejects unsupported specifiers, unresolved or unsafe paths, nonliteral dynamic imports, symlinks, non-`.mts` source files, overlapping/existing output directories, and type errors before output creation. The fixture in the existing `test/package.test.mjs` executes generated JS and keeps the current CommonJS CLI runnable. No compiler-generated `.mjs` is written, even transiently. This is a foundation, NOT a source policy, whole-tree migration, Node 20 proof, or completion of TM-01.
- [x] **TM-01b — Source-side Node 20 PR verification harness (child #2 only).** The archetype-only `pull_request` workflow checks out the PR merge commit without persisted credentials, requests only `contents: read`, pins checkout/setup-node to the MAINTAINERS.md SHAs, and selects Node 20.19.0 with Corepack pnpm 12.4.2. It runs typecheck, the full tests (including executable typed-emitter/CLI fixtures), a focused emitter test, offline package-consumer tests, and determinism. The static workflow checker and negative fixtures guard the entire read-only workflow contract. This checkbox means the harness was built and tested locally, **not** that a Node 20 run has passed; TM-01 and generated-support policy remain open.
- [ ] **TM-02 — Add deterministic source/generated policy and fixtures.** Route: focused direct implementation. Trigger evidence: no current extension/type policy, `scripts/check-determinism.mjs` is source-only, while inherited checkers move into `.factory/scripts/`. Implement separate source, payload, and generated-project inventory rules with sorted path diagnostics; pin a real executable entrypoint and include a local generated-project fixture. Add ordinary positive and negative functional tests without requiring a strict RED/GREEN sequence. Checks: `pnpm run test:module-policy` and `pnpm run check:module-policy` on source and fresh target; reject a staged rogue `.mjs` and untyped `.js` without hiding inherited support paths. Delay unconditional final enforcement until migration catches up; transitional work must not falsely claim a green repository policy.
- [x] **TM-02b — Source-side inventory adapter (child #4 only).** `src/module-policy-source.ts` collects staged tracked paths and Git modes via argv `git ls-files --stage -z`, validates regular files and every directory component without following symlinks, and reconciles the checked-in payload manifest with the payload path declaration and tracked identities. Callers must provide an absolute, trusted Git executable outside the repository; both Git calls disable fsmonitor and use an isolated environment (see correction checkpoint below). `checkSourceModulePolicy` passes only these source inventories to the pure policy, with empty generated/application/provenance mappings. Offline repository fixtures in `test/package.test.mjs` cover ordering, exact modes, nested modules, duplicates, escapes, and symlinks. The final TM-02 gate, generated application ownership, and build-derived compiler provenance remain open.
- [x] **TM-02c — Generated-project classification adapter (child #5 only).** Recompute the actual composed creator plan from the packaged payload and exact initialization config; reconcile planned file identities, state bytes, and disk, then scan all of `.factory/` for undeclared support files. Outside `.factory/`, inspect only exact planned output paths. Do not infer application ownership or typed compiler provenance from editable state. Functional tests cover the fresh generated baseline and fail-closed drift. This is not a passing migration gate.
- [x] **TM-02d — Prove the first tracked compiled source mapping (this local child only).** Route: delegated bounded WRITE on `feat/issue-181-typed-provenance` from `4dd8867`, following draft PR #204's `feat/issue-181-first-typed-checker` branch; no remote delivery. Source policy inventories staged tracked identities using the existing trusted external Git executable, then calls the typed runtime verifier on the actual working-tree source and runtime. Only after a fresh compiler output file-set and byte comparison does it classify `scripts/typed/check-real-agent-workflow.mts` -> `scripts/typed-runtime/check-real-agent-workflow.js`. If both are absent in a disposable repository, the previous policy applies; a partial pair, wrong mode, symlink, altered bytes, or extra runtime file fails closed. Other tracked `.js` and `.mjs` files retain sorted diagnostics. This does not complete TM-02 or enable the final gate.
- [x] **TM-02e — Generalize tracked compiler provenance (this local child only).** Route: owner-delegated bounded WRITE on `feat/issue-181-multi-typed-provenance` from `f7f4301`, following draft PR #205's parent branch; no remote delivery. Match every `.mts` under exact source-only `scripts/typed/` to its relative `.js` under `scripts/typed-runtime/`, including nested paths, with one root `package.json` declaring ESM. Inventory the Git index using the existing trusted external Git executable with fsmonitor disabled; require every source, emitted output, and manifest tracked regular mode `100644`. The existing verifier recompiles the actual source tree into disposable output and compares the complete emitted file set and every byte against committed runtime before mapping any pair. Refuse partial sets, untracked on-disk entries, unexpected reserved-runtime JavaScript, symlinks, modes and byte drift. Unrelated disposable repositories without either tree retain their previous behavior; unrelated JavaScript outside the reserved runtime still receives sorted diagnostics. Focused two-module Git fixture and sequential local checks below; no final policy alias or whole-tree green claim.
- [ ] **TM-03 — Migrate source-only automation and tests in compatible slices.** Route: direct work-unit migration, each slice with its adjacent tests. Trigger evidence: 34 `scripts/*.mjs`, 31 `test/*.test.mjs`, release/real-agent workflows with literal filenames, and `pnpm test` using a `.mjs` glob. Update typed imports, test-runner globs, scripts, shell/Node invocations, static workflow checks, and runtime catalog references together; keep shell-free entrypoints and original behavior. Checks: focused `node --test` runner using the verified new command, workflow contract/self-checks, determinism, then `pnpm test` at each executable slice; runtime harness uses offline fixtures, never hosted resources.
- [x] **TM-03a — First cold executable source-only checker.** Route: delegated direct, one local-only child at `f8e5802` on `feat/issue-181-first-typed-checker`. Replace only the real-agent workflow checker with `.mts` source and committed compiler-derived ESM `.js` plus nested `type: module`; update deterministic importer, active docs, source-only ownership and Node20 cold pre-build check together. Build recompiles into a disposable directory, checks exact file set including `package.json` and byte equality before packaging, and never repairs tracked JS. Checks: cold new checker and determinism before build; build/typecheck, focused workflow/package tests, full suite, workflow contract, package consumer, static workflow/layout and diff check. Rollback: revert this child work-unit (source/runtime pair, verifier/build hook, importer, workflow/static checker/tests, docs/ownership and payload lock), without changing inherited startup or other migration children. After this slice the tracked baseline becomes **65 `.mjs`** (33 scripts, 31 tests, root startup) and **one tracked `.mts` and one compiler-derived `.js`**; generated-project baseline still has five `.mjs`. The complete source/generated module-policy gate remains failing until further migrations and builder-derived policy provenance are implemented; `check:module-policy` is not claimed green. Hosted Node20 verification of this child is pending a PR.
- [x] **TM-03b — Typed resource proof and cleanup eligibility leaves.** Route: owner-delegated local work unit on `feat/issue-181-typed-proofs` from `8179441`, following draft PR #206's parent. Move exactly two source-only leaves into `scripts/typed/`, retain their error and result contracts, and import their committed compiler-derived ESM `.js` from the two existing source importers and three focused test files. Extend the runtime file-set fixture, register source and runtime paths as removed ownership, and derive the payload lock with the builder. No shim, new `.mjs`, inherited payload, #184 helper, or bootstrap workflow edit. Checks and rollback below; no whole-tree green policy claim.
- [x] **TM-03c — Typed resource-proof cleanup importer (local implementation only).** Route: one bounded local source-only importer migration on `feat/issue-181-typed-cleanup-importer` from integrator `b65a0f748308afa511d037240a4af71abed0bc79`. Replace `scripts/resource-proof-cleanup.mjs` with `scripts/typed/resource-proof-cleanup.mts` and committed compiler-derived `scripts/typed-runtime/resource-proof-cleanup.js`; retarget its actual workflow/test consumers and exact static contract. Preserve offline cleanup status/error behavior. Extend the complete runtime file-set fixture, register both new paths as removed ownership, and derive the payload lock after all inputs. No shim, inherited payload/startup, npm release workflow, #184 logic, hosted run, or complete policy gate. Local evidence and rollback are recorded below; TM-03 and #181 stay open.
- [x] **TM-03d — Typed resource provisioning importer (local implementation only).** Route: one bounded source-only work unit on `feat/issue-181-typed-provision-importer` from clean `fce70e05a125166d49fb7bb9d32f506853532f6c` (draft child PR #216; integrator #210). Replace only `scripts/resource-provision-and-proof.mjs` with typed source and committed compiler-derived runtime JS; retarget its bootstrap workflow and focused test consumer, update the bootstrap static checker/mutations and exact runtime file-set fixture, register both new paths as removed ownership, and derive the combined payload lock. Preserve create-once, readback and persisted-proof boundaries with offline fixtures. No shim, inherited payload/startup, #184 or hosted calls. Sequential local evidence and rollback are recorded below; TM-03 and #181 remain open.
- [ ] **TM-04 — Migrate root startup and inherited generic assets atomically.** Route: direct payload/ownership work unit. Trigger evidence: `start.mjs` is both a root-discovered payload entry and referenced by hooks, docs and generated project checks; four generic checkers are payload entries and relocated on initialization. Update startup/hook paths, docs, ownership, payload declaration/lock, creator/runtime consumers, and generated CI check together, preserving host-required `.ts` plugin entrypoints. Checks: `node start.mjs --self-check` and `node scripts/check-factory-layout.mjs` **before** cutover; verified replacement commands, ownership/payload checks, `pnpm build`, `pnpm test`, offline packed apply/verify, generated startup/policy and rerun `noop` **after** cutover. Do not regenerate the payload lock until the new input paths and bytes are final for the slice.
- [ ] **TM-05 — Finish type-safe CLI/build integration and normal verification gate.** Route: direct integration work unit. Trigger evidence: CommonJS `src/*.ts` CLI, `scripts/build.mjs` invoking `tsc` and chmod on `dist/index.js`, `package.json` bin/test scripts, and generated CI recipe composition. Preserve package `foundry` CLI behavior and required `.ts` host integrations; wire policy to the actual source verification path and generated project without making language-neutral CI recipes assume a TypeScript application. Checks: `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm run check:module-policy`, `pnpm run test:module-policy`, `pnpm test:package-consumer`, executable `foundry --version/--help`, and a generated-project CI/check-path assertion.
- [ ] **TM-06 — Audit inventories, documentation, and delivery evidence.** Route: direct bounded audit / review. Trigger evidence: `README.md`, `AGENT.md`, `MAINTAINERS.md`, `docs/factory-layout.md`, `docs/determinism.md`, `docs/creator.md`, workflow command strings, tests, and payload lock all reference active filenames. Update only active paths, not historical records; inspect tracked paths, generated tree, packed payload, emitted build output and references. Checks: full applicable commands above, sorted policy output for both targets, workflow and ownership checks, `git diff --check`, source/generated no-`.mjs` proof, typed `.ts` exception proof, exact review-size count, runtime harness result or explicit N/A, and per-slice rollback boundaries. Capture results before claiming verification or delivery.

## Review workload and delivery strategy

Selected `delivery_strategy`: **feature-branch-chain** for #181, as chosen by the owner after planning. Integrator branch `feat/issue-181-integrator` starts at main `d6b0f68`; child #1 `feat/issue-181-typed-modules` is the isolated TM-01a typed-emitter foundation, targeting that integrator. Later children depend on verified Node 20.19 and package/generated output proof before any broad migration. The earlier local-only slice did not include a PR or tracker remote write; the later delivery checkpoint is below. Source-extension migration alone touches 66 files / 7,834 lines; a naïve delete/add presentation can show at least **15,668 authored changed lines** before specifier changes, policy, docs, manifests, and workflow wiring. Forecast **~16,000–20,000+ raw additions plus deletions** if renames are not recognized; even if Git recognizes clean renames, the review footprint remains 66 modules and their consumers. Measure authored additions plus deletions per child; do not code-golf tests/docs to fit 400 lines.

TM-01a output classification: this tooling lives under the already registered `creator_packaging` `src/` ownership directory; its functional tests are under the registered `test/` directory and this task under `odd/`. No payload input changes or lock regeneration. The emitter is compiled to CommonJS `dist/typed-module-emitter.js` by the existing build; it is tooling, not an ESM implementation module or an initialized-project asset. Only JS written under a fresh nested `type: module` directory by this compiler callback is eligible as type-derived ESM output. Current `dist/index.js` stays CommonJS, with no package-wide `type` switch. TypeScript 5.8 and Node v26.9.0 validate only this isolated fixture; Node 20.19 is not installed here. Test Node 20.19 and real payload/generated consumers before turning on a repository-wide policy or converting existing modules.

TM-01a verification (local Node v26.9.0): `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test --test-name-pattern='typed module emitter' test/package.test.mjs` 2/2 pass (executable ESM returned `80`, existing CommonJS CLI help still works, generated fixture inventory contains only `.js` and `package.json`); `pnpm test` 175/175 pass; `node scripts/check-determinism.mjs` reported `determinism and Python-removal audit OK`; `git diff --check` exit 0. Rollback boundary: remove `src/typed-module-emitter.ts` and the two focused cases in `test/package.test.mjs`, then revert this task-plan update; no existing source, payload, or CLI behavior was changed. Node 20.19 validation, final policy, and complete package/generated inventory remain pending.

Child #1 implementation commit: `de6c021` (236 authored additions, no deletions); this passive evidence line is a separate local documentation follow-up, with no code or verification change.

### Passive delivery checkpoint (child #1)

- Owner-selected separate **feature-branch-chain**: integrator `feat/issue-181-integrator` was pushed from `d6b0f68`; its tracker PR is still pending because the integrator has no diff against its base. Child #1 is draft [PR #188](https://github.com/eff3ct0/agent-foundry/pull/188), head `feat/issue-181-typed-modules` at `a87ad2d9d3bd9e432cfcd4b19488e7724680cf8b` before this docs follow-up, base `feat/issue-181-integrator`, and sole `type:*` label `type:feature` (PR readback). Do not merge or mark the tracker ready.
- Parent readback reported governance `validate` **SUCCESS** for PR #188. This is governance-only evidence, **not** CI testing on Node 20.19 or validation of startup, payload, generated output, or module policy. Node 20.19 source/runtime proof, complete policy and migration remain pending; TM-01 remains unchecked and issue #181 stays open.
- Checkpoint verification: structural PR head/base/draft/label readback and local task-file/diff checks only; no new code test or runtime harness for this docs-only follow-up (N/A: no executable behavior changed). Child #1's prior local Node v26.9.0 test evidence above is historical and does not establish Node 20 compatibility. Rollback for this checkpoint: revert only this task-file docs commit; do not revert `de6c021` or modify the integrator/PR. The prior TM-01a implementation rollback boundary remains recorded above.

Safe sequence: TM-01a establishes the isolated emitter; next prove Node 20.19/runtime and package/generated compatibility; add the policy with functional fixtures but keep its final all-green gate pending; convert coherent script/test/dependency clusters with their checks; migrate inherited startup and payload paths together; then enable unconditional source and generated CI gates and audit the final inventories. Each child must keep its executable boundary runnable, with tests/docs and an independently stated rollback; do not rename source files just to meet a checkbox. If a cohesive later slice cannot fit 400 authored lines after one honest pass, report the overage and request `size:exception` rather than shrinking tests or code.

## TDD mode, progress, and next action

- TDD mode: **ordinary functional tests, not strict RED/GREEN TDD**, per the owner's explicit selection for #181. Use `pnpm test` and `pnpm typecheck` as the normal test/typecheck runner, plus the focused and end-to-end commands above when applicable. `docs/engineering-handbook.md` still says `<TDD_POLICY>` and `templates/agent-runbook.md` names the `TESTING/TDD` phase; neither overrides this issue-specific selection. Issue #112's separate TDD-off decision is not the source of this choice.
- Progress: read the issue plan and project contracts; inventoried tracked, payload, build, test, workflow and generated boundaries. The isolated TM-01a emitter and functional tests have been implemented and verified locally on Node v26.9.0. A full source/generated policy has not been implemented or run; baseline inventories remain discovery evidence, not passing policy evidence.
- Next action after this passive checkpoint: obtain Node 20.19 source/runtime proof for typed startup and generated package paths, then implement TM-02 positive/negative policy fixtures and progressively migrate existing modules in reviewable children. Keep PR #188 draft and the integrator tracker pending until their actual evidence gates are met. The emitter's isolated fixtures do not establish Node 20.19 compatibility or a passing source/generated policy; do not close issue #181.

### TM-01b local checkpoint — separate feature-branch-chain child

- Child #2 starts at child #1 head `83d6fdd` on `ci/issue-181-node20-check` in `/home/steam/git/agent-foundry-worktrees/issue-181-node20`. Selected delivery strategy remains **feature-branch-chain**: integrator -> child #1 (`feat/issue-181-typed-modules`, draft PR #188) -> child #2 (this branch). No push or PR was requested for this child; a future PR must target the preceding child branch, and actual workflow execution must be observed rather than presumed. The workflow is source-only (`archetype_only_release_e2e`), not a generic initialized-project recipe or a release workflow. GitHub documents that `pull_request` runs the merge commit with fork protections, whereas `pull_request_target` uses the trusted base and elevated context: https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target.
- Runtime check: on local **Node v26.9.0**, `pnpm build` exit 0 after lock regeneration; `pnpm typecheck` exit 0; `node scripts/check-bootstrap-workflow.mjs` exit 0; `node --test test/check-bootstrap-workflow.test.mjs test/package.test.mjs` 26/26 pass; `pnpm test` 176/176 pass; `pnpm test:package-consumer` 3/3 pass; `node scripts/check-determinism.mjs` exit 0; focused `node --test --test-name-pattern='typed module emitter' test/package.test.mjs` 2/2 pass; `node scripts/check-factory-layout.mjs` exit 0; `git diff --check` exit 0. No lint/format gate is configured. The initial build failed as expected after changing ownership: `archetype-ownership.json` is itself a declared payload file; `node scripts/build-payload.mjs --write-lock` regenerated only its checksum and the payload digest in `package/payload-manifest.json`, then the build and tests passed.
- **Pending evidence:** this local run is NOT Node 20.19.0 proof. Wait for the child PR's actual `pull_request` Node 20.19.0 check before claiming typed-emitter, CLI, or packed-consumer compatibility. The workflow exercises offline generated-tree identity through the package-consumer test, but does not establish the final no-`.mjs` generated-support policy or fresh-project checker; TM-01 and issue #181 stay open. Rollback boundary: remove `.github/workflows/archetype-node20.yml`, revert its static checker and fixtures, its single ownership registration, the builder-derived lock change, and this TM-01b checkpoint; TM-01a remains untouched.
- Child #2 implementation work-unit commit: `201a33d07117f5ecb813817f47784e3aa0a9c93e` (109 additions + 7 deletions = **116 authored lines**, including tests and task documentation). This passive commit-reference follow-up changes no executable contract and needs no additional runtime harness (N/A); its rollback is only this evidence line. No child PR or Node 20 run has yet been observed.

### PR #192 hosted readback and scoped toolchain correction

- Hosted draft PR #192 `verify` **FAILED before tests** on Node 20.19.0: Corepack installed pnpm@12.4.2, but its shim expected a CLI file absent from the installed package. Public pnpm@12.4.2 metadata declares the `pnpm` CLI bin and Node >=18. This failure is toolchain evidence, **not** a typed-emitter or Node 20 test result. The initial governance `validate` failed while the PR label was not yet applied; subsequent `validate` **SUCCESS** needs no governance change.
- This source-only `pull_request` correction replaces Corepack activation in `.github/workflows/archetype-node20.yml` with exact `npm install --global pnpm@12.4.2`, followed by the existing Node 20.19.0 and pnpm 12.4.2 assertions and `pnpm install --frozen-lockfile`. The exact static contract and mutations reject restoration of the Corepack method, unpinned npm installation, or altered version assertion. Read-only permissions, credential-free checkout, action SHAs, and all test commands remain unchanged. The separate #184 release workflow also uses the failing Corepack command, but is **out of scope** for this correction; no release or publication claim is made.
- Local verification on **Node v26.9.0** (not Node 20): `pnpm build` exit 0; `pnpm typecheck` exit 0; `node scripts/check-bootstrap-workflow.mjs` exit 0 (all six static checks OK); `node --test test/check-bootstrap-workflow.test.mjs test/package.test.mjs` exit 0 (26/26); `pnpm test` exit 0 (176/176); `pnpm test:package-consumer` exit 0 (3/3); `git diff --check` exit 0. Focused runtime harness: offline packed package consumer, 3/3; hosted Node 20 rerun **pending**. No lint/format gate is configured.
- Next: rerun/read back PR #192's `pull_request` `verify` on hosted Node 20.19.0 after delivery, then evaluate the actual typed-emitter/CLI/package-consumer results. Keep TM-01 and issue #181 open; do not infer a hosted pass from this local correction. Rollback boundary: revert only this workflow's npm activation line, its exact static contract and mutation fixtures, and this checkpoint; the rest of TM-01b and all release/governance workflows remain independent.

### TM-02a policy-core child (bounded foundation, not the TM-02 gate)

- Parent hosted readback: draft PR #192 `verify` job **107313930489** at head **6faf721** reported **SUCCESS** on **Node 20.19.0**. Checkout/setup/toolchain assertions, typed-emitter tests and offline package-consumer tests passed in that job. This proves that bounded source-side fixture and packed-consumer path on hosted Node 20; it does **not** prove no-`.mjs` policy or type-derived output across all generated modules. The earlier pending statements above describe the state before that readback.
- Child #3 starts at `6faf721` on `feat/issue-181-module-policy` in `/home/steam/git/agent-foundry-worktrees/issue-181-policy`, following the separate feature-branch-chain. `src/module-policy.ts` is source-only CommonJS TypeScript tooling under the existing `creator_packaging` `src/` ownership directory; its cases extend `test/package.test.mjs` under existing `test/` ownership. No ownership input or payload path changes, so no payload lock regeneration.
- Pure `checkModulePolicy` consumes explicit per-file inventories for tracked source, payload, and generated tree, plus exact generated application-owned file identities and source-to-output typed compiler mappings. It rejects `.mjs` and unmapped `.js`/`.cjs`/`.jsx` in archetype-owned inventories, returns sorted scoped diagnostics, and fails closed on malformed paths, symlink-marked entries, duplicate identities, or invalid mapping. Typed `.ts` host integrations pass unchanged. An adapter must collect complete inventories using symlink-aware filesystem checks and derive provenance from the actual build; arbitrary caller declarations are not independent compiler proof. The application exception is per-file, never an extension or whole-directory exemption.
- This child intentionally does **not** expose `check:module-policy`, enable CI enforcement, inventory the present repository, or claim TM-02 complete: 66 tracked `.mjs` files and inherited generated scripts remain to migrate. Runtime fixture: the existing typed emitter writes and executes `.js` from `.mts`; a neighboring untyped generated support `.js` is rejected while exact application-owned code is excluded.
- Verification on local Node **v26.9.0**: `pnpm build` exit 0; `pnpm typecheck` exit 0; focused `node --test test/package.test.mjs` **11/11 pass**; `pnpm test` **179/179 pass**; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `git diff --check` exit 0. The focused runtime fixture executes an emitted `.js` file and prints `typed startup`; this is local Node 26 proof, not a hosted Node 20 run of this child. Rollback boundary: remove only `src/module-policy.ts`, the three policy cases/import in `test/package.test.mjs`, and this TM-02a checkpoint; leave the TM-01a emitter and TM-01b harness intact. Next: implement symlink-aware complete inventory/provenance adapters and migrate remaining modules before adding any unconditional gate.

### TM-02b source-inventory child (no repository gate)

- Parent PR #195 policy-core `verify` was reported **SUCCESS** at head **425f4b1**, Node 20, readback run **35901893088**. This is parent-only evidence; it does not validate TM-02b on Node 20. No remote operation was performed in this child.
- Child #4 starts at `425f4b1` on `feat/issue-181-policy-runner` in `/home/steam/git/agent-foundry-worktrees/issue-181-runner`. Selected strategy remains separate feature-branch-chain. The source adapter inventories only staged tracked files and the checked-in payload manifest/declaration; it never discovers a generated target or accepts caller-provided ownership/provenance exemptions. The source fixture uses a disposable local Git repository and a real `git add`; staged `.js`/`.mjs` modules are reported, untracked files do not enter the inventory, and symlinked components, nonregular Git modes, mismatched executable bits, malformed paths, and duplicate payload identities fail closed.
- Source-tree readback via `checkSourceModulePolicy(process.cwd())` found **66 tracked and 5 payload `.mjs` violations** (71 scoped diagnostics, zero generated entries). This is an expected failing policy baseline, not a clean result. Keep `pnpm test` independent of this check until migration; do not add `check:module-policy`/`test:module-policy` aliases or CI enforcement in this child. Generated-project application classifications and actual compiler-produced provenance require a later coherent work unit.
- Local verification is Node 26 only; the parent Node 20 run above does not transfer to this child. Rollback boundary: remove `src/module-policy-source.ts`, the source-adapter fixtures/import in `test/package.test.mjs`, and this TM-02b checkpoint; TM-02a's pure checker and earlier children stay intact. Next: collect authoritative generated-project file ownership and type-derived outputs, migrate inherited `.mjs`, then enable a green repository/generated gate.
- Verification on local **Node v26.9.0 / pnpm 12.4.2**: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test test/package.test.mjs` **13/13 pass** (offline Git-index/symlink runtime fixture); `pnpm test` **181/181 pass**; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `git diff --check` exit 0. A direct source-adapter readback reported 66 tracked plus 5 payload diagnostics. No Node 20 job was run for this child; lint/format gates are not configured. No payload ownership inputs or lock were changed.

### TM-02b security correction before delivery

- Independent verification found that argv-only `git ls-files --stage -z` executed a disposable repository's configured `core.fsmonitor` program; `git rev-parse --show-toplevel` did not. Inherited `PATH` could also select a hostile `git` executable. These were introduced by the child #4 source adapter at `824133e`; previous positive inventory tests did not cover process execution. This is a security correction, not a new source-policy gate.
- `inventorySourceModules` and `checkSourceModulePolicy` now require a caller-supplied absolute trusted Git executable outside the inspected repository. The adapter resolves it to a regular executable and rejects missing, relative, or repository-contained paths before invoking Git. Both Git commands use the resolved executable, argv-only `-c core.fsmonitor=false`, and a minimal environment with no inherited `PATH`, `HOME`, `XDG_CONFIG_HOME`, or `GIT_*` overrides (system config disabled). The caller remains responsible for selecting a genuinely trusted binary; this does not authenticate arbitrary executable bytes outside the repository.
- The disposable hostile fixture establishes that unguarded `rev-parse` leaves the marker absent and unguarded `ls-files` creates it, then removes it. In a subprocess with a shadow `git` first in `PATH` and an injected `GIT_CONFIG_COUNT` fsmonitor override, guarded inventory returns the expected `scripts/nested/start.mjs` while both marker and shadow remain absent. Missing/relative/in-repository executables and malformed local Git config fail closed. No Git commands target a user repository in this fixture.
- Verification on local Node **v26.9.0**: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test test/package.test.mjs` **15/15 pass**; `pnpm test` **183/183 pass**; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `git diff --check` exit 0. Hosted Node 20 and a complete migration policy remain pending; preserve the earlier child #4 and parent evidence above. Runtime harness: the hostile disposable-repository subprocess passed. Rollback boundary: revert only the trusted-Git invocation changes in `src/module-policy-source.ts`, the corresponding fixtures in `test/package.test.mjs`, and this checkpoint (doing so reopens the fsmonitor execution defect). No payload inputs or lock were changed.

Correction work-unit commit: `01709ca75bece981c870be232046095e835bcd4b`; child #4 cumulative authored diff vs `425f4b1`: **326** additions plus deletions (325 additions, 1 deletion), within the 400-line advisory. No push, PR, issue comment, merge, or remote action in that correction checkpoint.

### TM-02c generated-classification child (no repository gate)

- Child #5 originally started at TM-02b `824133e`, then was rebased locally onto corrected child #4 `01709ca75bece981c870be232046095e835bcd4b` on `feat/issue-181-generated-inventory` in `/home/steam/git/agent-foundry-worktrees/issue-181-generated`; separate feature-branch-chain remains selected. `src/module-policy-generated.ts` calls `preparePlan` with the same initialization config and optional agent selection, rather than trusting caller-owned paths or editable state. It requires every planned operation to be `noop`, allows only creator's unknown-file diagnostics (so unrelated application files do not become ownership exemptions), checks exact state bytes and all planned files against disk, and walks reserved `.factory/` without following symlinks. The pure policy receives zero generated application or compiled mappings. This adapter is source-side tooling, not a generated payload or new CLI command; no ownership or payload lock inputs change.
- Offline functional target: `foundry plan` -> `apply` -> `verify` -> rerun `apply` (`noop`), with GitHub Actions TypeScript CI and typed OpenCode plugin composed. The adapter returns **five** sorted generated `.mjs` diagnostics: `.factory/scripts/check-delivery-contract.mjs`, `.factory/scripts/check-factory-layout.mjs`, `.factory/scripts/check-pr-governance.mjs`, `.factory/scripts/sync-github-labels.mjs`, `start.mjs`. `app/own.js` stays outside the inventory; `.factory/scripts/nested/rogue.js` is reported; symlinked nested/owned files, missing owned files, and edited creator state fail closed. No arbitrary directory/application allowlist or compiled `.js` exemption is added.
- Local verification on Node **v26.9.0**: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test --test-name-pattern='generated module inventory' test/creator.test.mjs` **1/1 pass**; `pnpm test` **182/182 pass**; `pnpm test:creator` **22/22 pass**; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `git diff --check` exit 0. Initial focused run exposed missing generated CI when CI was not selected; the fixture now explicitly selects GitHub Actions/TypeScript and passed the rerun. No Node 20 verification for this child, new CLI alias, unconditional green policy/CI gate, or typed-build provenance proof is claimed. Next: migrate remaining `.mjs` modules and derive compiler output provenance before enabling the gate. Rollback boundary: remove the generated adapter, its single `test/creator.test.mjs` functional case/import, and this TM-02c task entry/checkpoint; TM-02a/b remain unchanged.

### TM-02c corrected-parent local synchronization

- Rebasing the unpushed child #5 commit `f7cdb25` onto corrected child #4 `01709ca75bece981c870be232046095e835bcd4b` produced `c9a58cc` on `feat/issue-181-generated-inventory`. The task-file conflict was resolved by retaining both the TM-02b security correction and TM-02c checkpoint; the trusted-Git source adapter and its hostile-repository regression tests remain in the parent, and child #5 retains its generated adapter and creator fixture. No other worktree or remote was changed.
- Reverified locally on Node v26.9.0 after the rebase: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test --test-name-pattern='generated module inventory' test/creator.test.mjs` **1/1 pass**; `node --test test/package.test.mjs` **15/15 pass** (including both hostile-Git fixtures); `pnpm test` **184/184 pass**; `pnpm test:creator` **22/22 pass**; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `git diff --check` and `git diff --check 01709ca...HEAD` exit 0. Child #5 code-and-checkpoint diff at `c9a58cc` vs `01709ca`: three files, 163 additions and 1 deletion (164 authored lines). No hosted Node 20 check or whole-tree module-policy pass is claimed. This evidence-only follow-up changes no runtime behavior; rollback is this documentation paragraph alone.

### TM-03a local executable migration checkpoint

- On local Node v26.9.0 / pnpm 12.4.2, **before any `pnpm build`**, `node scripts/typed-runtime/check-real-agent-workflow.js` printed `real-agent workflow static check OK`; `node scripts/check-determinism.mjs` printed `determinism and Python-removal audit OK`. The new checker is cold-runnable without a runtime loader or generated `dist/` prerequisite. An isolated `tsc` compilation of the existing emitter produced the two-file checked-in runtime byte-for-byte in a fresh disposable directory before the first build; both files were compared using `cmp`.
- After offline dependency installation and the builder-derived payload lock: `pnpm build` exit 0; `pnpm typecheck` exit 0; focused `node --test test/check-bootstrap-workflow.test.mjs test/package.test.mjs` **34/34**; `pnpm test` **185/185**; `pnpm test:workflow-contract` **18/18**; `pnpm test:package-consumer` **3/3**; `node scripts/check-bootstrap-workflow.mjs`, `node scripts/check-factory-layout.mjs`, and `git diff --check` exit 0. The focused runtime fixture compares both generated files, mutates one byte, deletes the JS file, adds an extra file, changes source, and induces a type error; all invalid cases fail closed and emitted disk inventory contains no `.mjs`. One *concurrent* workflow-contract invocation failed `ENOTEMPTY` because three commands independently ran `pnpm build` in the same `dist/`; its sequential rerun passed 18/18, as did the sequential package-consumer rerun 3/3. Do not run independent builds concurrently in one worktree.
- No source-to-output policy mapping is yet derived from this build proof: staged source-policy readback reported **65 tracked `.mjs`, five payload `.mjs`, and one unclassified tracked `.js`** (71 diagnostics); final `check:module-policy` remains pending. No hosted Node 20.19.0 run of this child exists without a PR. No release, provider, or inherited startup path was changed.

### TM-03a pre-verification execution correction (local-only)

- Independent verification of the 34/34 focused tests found a provenance-order defect: the Node 20 PR workflow ran committed `scripts/typed-runtime/check-real-agent-workflow.js` and `scripts/check-determinism.mjs` before `pnpm test` invoked `pnpm build`, which byte-compares fresh typed compiler output against the committed runtime. The determinism script imports the generated JS directly, so moving only the explicit checker invocation would not close the gap.
- The hosted workflow now runs `pnpm typecheck`, then `pnpm test` (including the fail-closed build verification), then the direct typed-runtime checker, focused emitter and package-consumer tests, and finally determinism. The exact static workflow contract and negative mutations reject moving either the direct invocation or the determinism importer before the verifying build while preserving both commands. Standalone cold `node scripts/check-determinism.mjs` and direct checker execution remain functional checks, **not provenance proof**; do not claim that running them before a build authenticates committed JS.
- Sequential local verification on **Node v26.9.0 / pnpm 12.4.2** after correcting the static expected text: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test test/check-bootstrap-workflow.test.mjs test/package.test.mjs` **34/34 pass**; `pnpm test` **185/185 pass**; `pnpm test:workflow-contract` **18/18 pass**; `pnpm test:package-consumer` **3/3 pass**; `node scripts/check-bootstrap-workflow.mjs` six static checks OK; `node scripts/check-determinism.mjs` printed `determinism and Python-removal audit OK`; `git diff --check` exit 0. The first two focused attempts failed 32/34 because the expected-text indentation differed; fixed and reran the complete sequence successfully. No hosted Node 20 run of this child until a PR; no final module-policy gate enabled.
- Focused runtime harness: the package fixture builds and byte-compares fresh typed output, and the workflow contract mutation fixtures reject both pre-build direct and indirect execution. Rollback boundary: revert only this workflow order change, its exact static contract, its pre-build-order fixture mutations, and this checkpoint; reverting reopens the provenance-order defect. No release workflow, inherited startup path, generated runtime bytes, or payload lock changed by this correction.
- Child authored diff vs `f8e5802` (excluding committed generated JS): **140** additions plus deletions (123 additions, 17 deletions); this correction is part of the same TM-03a work unit, not a completed migration or hosted verification.

### TM-02d local compiler-provenance checkpoint

- Starting inventory: the staged Git index contains both exact source/runtime paths as regular mode `100644`; the declared payload contains neither path and still has five `.mjs` files. Source policy derives no mapping from an index hash, filename alone, or a caller-supplied classification. Each call recompiles the actual working-tree source to a fresh disposable output and compares the complete runtime directory, including `package.json`, byte-for-byte. The verifier returns the proven output set only after comparison succeeds; the adapter maps the one exact pair only when that set is exactly `check-real-agent-workflow.js` and `package.json`.
- Focused offline Git fixture: **17/17** package tests pass. An unrelated repository with neither path retains its former diagnostics; a staged valid pair excludes only the compiled JS. Staged neighboring rogue `.js` and `.mjs` remain diagnosed in order; extra JS in the runtime directory, modified JS or source, missing runtime, executable-mode drift, symlinked runtime, and either half missing from the index fail closed. No live repository was mutated by these hostile cases.
- Sequential local verification on Node v26.9.0 / pnpm 12.4.2: `pnpm build`, `pnpm typecheck`, `node --test test/package.test.mjs` (17/17), `pnpm test` (186/186), `pnpm test:package-consumer` (3/3), `node scripts/check-determinism.mjs`, `node scripts/check-bootstrap-workflow.mjs`, and `git diff --check` all exited 0. Direct source-root readback via `checkSourceModulePolicy(process.cwd(), "/usr/bin/git")` returned **70** sorted diagnostics: 65 tracked `.mjs`, five payload `.mjs`, zero tracked JS; the compiled runtime is not diagnosed. The former 71 included that runtime. No tests skipped, no observed failures, and no hosted Node 20 run in this child. Lint/format gates are not configured.
- Rollback boundary: revert only the TM-02d adapter mapping in `src/module-policy-source.ts`, verifier's proven-output return in `src/typed-runtime-verifier.ts`, its fixture in `test/package.test.mjs`, and this checklist/checkpoint. Keep TM-03a's runtime/build verification, the Git inventory safety guard, inherited startup, release workflow, payload lock, and final policy/CI gate unchanged. TM-02 and issue #181 remain open; further migration and final gating are separate work units.

### TM-02d tracked runtime manifest correction (local-only)

- Independent verification of `45a060a` found that the source adapter mapped the tracked `.mts` to the tracked `.js` even when `scripts/typed-runtime/package.json` was present on disk but removed from Git's index, or was staged executable (`100755`). The compiler verifier compared the on-disk manifest bytes but did not establish its tracked regular identity. This is a correction to TM-02d, not a new policy gate or a directory-wide exception.
- The adapter now requires the exact runtime manifest to be tracked regular mode `100644` before the fresh compiler-byte comparison grants the single source-to-runtime mapping. A lone manifest or incomplete source/runtime pair fails closed; a repository with none of these paths retains its prior policy diagnostics. The existing build verifier still checks the complete emitted file set and both runtime file contents byte-for-byte; an altered manifest remains rejected. The trusted external Git/fsmonitor guard is unchanged.
- The disposable Git fixture first reproduced the untracked-on-disk manifest bypass (focused test failed with `Missing expected rejection`), then passed after the correction. It removes only the manifest from the index with `git rm --cached`, stages mode `100755` through `git update-index --chmod=+x`, restores mode `100644`, and changes the manifest bytes; positive mapping, incomplete pair, neighboring rogue modules, and hostile Git cases remain covered. The executable-mode fixture does not rely on a Git filesystem-mode autodetection setting.
- Sequential local verification on Node v26.9.0: `pnpm build` exit 0; `pnpm typecheck` exit 0; `node --test test/package.test.mjs` 17/17 pass; `pnpm test` 186/186 pass; `pnpm test:package-consumer` 3/3 pass; `node scripts/check-determinism.mjs` exit 0 (`determinism and Python-removal audit OK`); `node scripts/check-bootstrap-workflow.mjs` exit 0 (six static checks OK); `git diff --check` exit 0. Runtime harness: the focused disposable Git fixture recompiles and byte-compares typed output without remote resources. No hosted Node 20 run or complete module-policy pass is claimed.
- Rollback boundary: revert only the manifest index guard in `src/module-policy-source.ts`, the added manifest cases in `test/package.test.mjs`, and this correction checkpoint. Do not revert the existing build-byte verifier, Git execution guard, payload lock, inherited startup, or earlier TM-02d mapping. Keep TM-02 and issue #181 open.

### TM-02e multi-module provenance checkpoint (local-only)

- Child starts at `f7f4301` on `feat/issue-181-multi-typed-provenance`; the owner authorized a separate issue chain but no remote action in this work unit. Reconcile this full task with Engram #4164 and #4371 (the latter continues the 50 KB-limited mirror) before editing. Historical checkpoints above remain unchanged. No generated project, inherited `.mjs`, CLI, #184 workflow, or payload input changed.
- The source adapter derives source/output pairs from the verifier's freshly compiled output names, checks exact Git-index file sets and mode `100644` for every `.mts`, `.js`, and the root ESM manifest, and rejects either reserved tree existing alone. The verifier still compares the entire output tree and bytes, including nested paths and manifest; it never repairs committed output. Offline two-module fixture uses a nested `.mts` dependency and tests untracked or wrong-mode output, untracked or wrong-mode manifest, missing source/output, extra reserved JS, symlink/mode/byte drift, and sorted rogue JavaScript outside the runtime. The separate no-pair fixture rejects unexpected reserved runtime JS; unrelated repositories with neither tree retain previous diagnostics.
- Sequential verification on local Node v26.9.0 / pnpm 12.4.2: `pnpm build` and `pnpm typecheck` exit 0; `node --test test/package.test.mjs` 18/18 pass; `pnpm test` 187/187 pass; `pnpm test:package-consumer` 3/3 pass; `node scripts/check-determinism.mjs` and `node scripts/check-bootstrap-workflow.mjs` exit 0 (six static checks OK); `git diff --check` exit 0. No failures or skips. The focused runtime harness recompiles two source modules in a disposable Git repository and exercises positive and negative provenance paths. Direct source-policy readback still returns **70 sorted diagnostics** (65 tracked and five payload `.mjs`, zero typed runtime), NOT a whole-tree pass; no hosted Node 20 run, final gate, or lint/format gate is claimed. The authored work-unit diff is three files, 89 additions and 28 deletions (117 lines) before this result line. Rollback: revert only the generalized mapping in `src/module-policy-source.ts`, the added/extended fixtures in `test/package.test.mjs`, and this TM-02e subtask/checkpoint. Preserve TM-02d's manifest protection, the verifier/build hook, trusted Git guard, and the remaining migration plan. TM-02 and issue #181 remain open.

### TM-03b resource-proof leaf migration (local checkpoint)

- The two leaves have no local imports and now live as typed `.mts` under `scripts/typed/`; their three `.js` runtime siblings share the existing `scripts/typed-runtime/package.json` ESM scope. Existing generalized compiler-byte verifier and Git-index source policy need no name exceptions. Cold pre-build direct imports of both new runtime leaves succeeded on local Node v26.9.0. The offline proof/cleanup fixtures make no hosted calls. `.github/workflows/bootstrap-e2e.yml` still reaches the leaves through the unmigrated importers, so its command paths did not change.
- After `node scripts/build-payload.mjs --write-lock` regenerated the ownership checksum and digest, sequential local checks exited 0: `pnpm build`, `pnpm typecheck`, `node --test test/resource-provisioning-proof.test.mjs test/resource-proof-cleanup.test.mjs test/resource-cleanup-eligibility.test.mjs test/package.test.mjs` (35/35), `pnpm test` (187/187), `pnpm test:workflow-contract` (18/18), `pnpm test:package-consumer` (3/3), `node scripts/check-bootstrap-workflow.mjs` (six static checks), and `node scripts/check-determinism.mjs`. The runtime fixture checks the exact three compiled JS files plus the ESM manifest and retains byte-drift, extra-file, missing-file, source-change, and type-error negatives. Source policy readback after staging reports **68 diagnostics**: **63 tracked `.mjs`**, five payload `.mjs`, and zero typed runtime diagnostics; this is NOT a whole-tree policy pass. Hosted Node 20 verification awaits a PR. No lint/format gate is configured.
- Rollback: revert only this TM-03b work unit (two `.mts`/`.js` pairs, the two removed `.mjs` leaves, importer and test specifiers, runtime fixture, ownership entries, builder-derived lock, and this checkpoint). Keep the TM-02e verifier/policy, earlier checker, inherited startup and payload, and #184 release helpers unchanged. The final TM-02 gate and issue #181 remain open.

### TM-03b interrupted-transport recovery and delivery checkpoint

- Recovered the existing 14 staged paths without reset, overwrite, or rebase; independent read-only verification inspected the complete staged diff and reproduced cold imports, fresh compiler-to-committed-runtime byte/file-set proof (three `.js` plus ESM manifest), ownership/payload checksum identity, and the expected 63 tracked plus five payload `.mjs` diagnostics with zero typed-runtime diagnostics. Authored review size excludes 198 generated JS lines: 146 additions plus 127 deletions = **273 lines**. The whole-tree module policy is still failing as expected.
- Sequential local Node v26.9.0 checks passed: `pnpm build`, `pnpm typecheck`, the four focused resource/package test files (35/35), `pnpm test` (187/187), `pnpm test:workflow-contract` (18/18), `pnpm test:package-consumer` (3/3), bootstrap static check (six checks), determinism, and staged/unstaged `git diff --check`. A separate bootstrap static spot check also passed. Builds changed only ignored `dist/`; the worktree was clean after commit. Hosted Node 20 and PR checks remain pending.
- Work-unit commit: `6ac6229f22ff08e85b7f10d28991e38ae7767d39` on `feat/issue-181-typed-proofs`, based on parent #206 head `8179441d8726d534f7324e61e95ff93afa5e6a6c`. RDD is globally off; native committed-range assessment reported `medium` (`configuration_change`) and `slice_budget_reached` on its raw 611-line view, which includes generated files. Independent local verification above is the check of record, not a native review receipt. Deliver this child as a draft PR against `feat/issue-181-multi-typed-provenance`; no merge, issue closure, or release readiness follows from it.

### Integrator local merge checkpoint (uncommitted)

- On `feat/issue-181-integrator`, preserve `HEAD` `9dfe1d80650aa4628c2b14f2071168feb5462b50` and `MERGE_HEAD` `5fe1a85ba31caa091c4d94c9baca22b6b9cbc381` (`refs/remotes/authorized/main-for-181`). The only merge conflict was the derived `package/payload-manifest.json`; `node scripts/build-payload.mjs --write-lock` regenerated and staged it with payload digest `sha256:a8f148da39cb35b310e26e71b231b6fccf9c23fa5907469454988068e4692e60`. No parent side was chosen, no merge commit or remote operation was made.
- Sequential local Node v26.9.0 checks after the merge: direct cold imports of `resource-provisioning-proof.js` and `resource-cleanup-eligibility.js`, the direct typed workflow checker, `pnpm build`, `pnpm typecheck`, four focused resource/package test files (35/35), `pnpm test` (194/194), `pnpm test:workflow-contract` (21/21), `pnpm test:package-consumer` (3/3), bootstrap workflow static check (six checks), determinism, factory layout, delivery-contract self-check, and startup self-check all exited 0. The requested cold-import literal `resource-proof-cleanup.js` does not exist: the initial invocation failed `ERR_MODULE_NOT_FOUND`; the tracked name above passed on rerun. Do not count that initial invocation as passing. Fresh `verifyTypedRuntime` recompilation matched the exact four-file runtime set (three `.js` and ESM `package.json`) and every byte.
- Direct source-policy readback with trusted `/usr/bin/git`: 68 expected diagnostics, 63 tracked `.mjs` plus five payload `.mjs`, zero typed-runtime diagnostics. This is NOT a whole-tree policy pass; final TM-02 gate, remaining `.mjs` migration, generated-project policy, hosted Node 20 and issue #181 acceptance remain open. No lint/format gate is configured. Rollback boundary: discard this uncommitted merge and its derived manifest/checkpoint as one integration unit only with explicit owner approval; do not independently retain a parent manifest or rewrite the historical child checkpoints. The source/runtime migration and prior child rollback boundaries remain independent.
- Independent read-only validation of the staged merge found no severe merge-introduced defect: all 62 payload paths matched the combined index, disk and lock; fresh compilation matched the three runtime JS files and ESM manifest; focused workflow/package tests passed 39/39, six workflow static checks and staged/unstaged diff checks passed. The inherited hosted journey parser at `.github/workflows/real-agent-journey.yml:205-206` splits the scoped package spec on every `@`, yielding an empty name; this line is identical in both parents. This separately testable blocker prevents claiming hosted release-journey readiness, but does not invalidate the offline merge checks. No hosted Node 20 or published-registry evidence was obtained.
- Integration work-unit commit: `33d8611a2f2fd67bd73a1390047623df1f4d194d`, with first parent `9dfe1d8` (#181) and second parent `5fe1a85` (#183/main). The verified merge tree contains the regenerated combined payload lock; this commit does not complete TM-02, #181, #184 or the release. This documentation-only SHA record has no executable runtime boundary (N/A); its rollback is this line alone.

### TM-03c local migration checkpoint (in progress)

- Branch `feat/issue-181-typed-cleanup-importer` was created from clean integrator head `b65a0f748308afa511d037240a4af71abed0bc79` in the typed-proofs worktree; the integrator ref is not to be changed. Scope: one source-only cleanup importer and its direct workflow/test/static-contract consumers, typed runtime, ownership and builder-derived lock. Prior ledger history remains authoritative. Verification and rollback evidence to be appended after implementation; do not claim a passing whole-tree module policy or hosted Node 20 result.
- The offline cleanup importer preserves canonical proof validation, one exact readback/deletion, recovery codes, and persisted artifact/error contracts. Its only direct consumers are the focused test and the cleanup step in `.github/workflows/bootstrap-e2e.yml`; the static checker and its negative fixture now enforce the new import. Source `.mts` imports sibling `.mjs` specifiers resolved by the existing emitter to sibling `.js` at runtime. Both new paths are source-only `removed` ownership, absent from `package/payload-files.json`; the builder-derived manifest digest is `sha256:449b7b7e5ff51e39bafe6539b393f64b4ec4c79e88fba4daad2dccd49299db03` with the same five `.mjs` payload paths. No npm-release, startup, or hosted behavior was exercised.
- Cold pre-build imports of the new cleanup JS and all three existing typed JS leaves passed on local Node v26.9.0. After the final compiler output and `node scripts/build-payload.mjs --write-lock`, sequential commands exited 0: `pnpm build`, `pnpm typecheck`, `node --test test/resource-proof-cleanup.test.mjs test/resource-cleanup-eligibility.test.mjs test/resource-provisioning-proof.test.mjs test/package.test.mjs` (**37/37**), `pnpm test` (**196/196**), `pnpm test:workflow-contract` (**21/21**), `pnpm test:package-consumer` (**3/3**), `node scripts/check-bootstrap-workflow.mjs` (six static checks), `node scripts/check-determinism.mjs`, `node scripts/check-factory-layout.mjs`, `node scripts/check-delivery-contract.mjs --self-check`, and `node start.mjs --self-check`. Initial emitter compilation reported TypeScript narrowing errors in the new importer; these were fixed before the final successful sequence. No lint/format gate is configured.
- Runtime harness: offline injected read/delete/storage fixtures cover exact deletion, already-absent, mismatches, invalid boundaries, download/mutation failures, and artifact write errors without credentials or hosted resources. Fresh `verifyTypedRuntime` recompilation matched **all five files byte-for-byte** (`check-real-agent-workflow.js`, `package.json`, `resource-cleanup-eligibility.js`, `resource-proof-cleanup.js`, `resource-provisioning-proof.js`). After staging, `checkSourceModulePolicy(process.cwd(), "/usr/bin/git")` reported **67 expected sorted diagnostics: 62 tracked `.mjs`, five payload `.mjs`, zero typed-runtime JS**. This is NOT a whole-tree policy pass. Staged diff: **11 paths, 230 additions plus 90 deletions; 232 authored changed lines excluding 88 compiler-generated JS lines**. Staged/unstaged `git diff --check` both exited 0; no tests/docs were removed to fit the advisory ~400-line review budget.
- Rollback boundary: remove only the new cleanup `.mts`/compiler-derived `.js` pair and restore the old `.mjs` importer, both direct import specifiers, static checker and negative fixture, cleanup error-contract tests, runtime file-set expectation, source-only ownership registration, builder-derived payload lock and this TM-03c checkpoint. Leave prior typed leaves, verifier/policy, inherited payload/startup and #184 unchanged. No commit, push, PR, issue closure, hosted Node 20 verification, or whole-tree `check:module-policy` gate is claimed; remaining TM-03 migrations, TM-02 final source/generated policy and #181 acceptance remain open.
- Independent read-only check of this exact staged work found no severe candidate-caused defect. Nine offline scenarios matched the previous importer's outcomes, error codes, and call traces; `node --test test/resource-proof-cleanup.test.mjs test/check-bootstrap-workflow.test.mjs test/package.test.mjs` passed **47/47**; six workflow static checks and both diff checks passed. The 62-file payload, mode/path inventory and five-file fresh compiler-byte identity matched the staged tree. Static workflow assertions cannot prove hosted execution; Node 20 and the final source/generated policy remain unverified.
- TM-03c work-unit commit: `0bbc95caf33ce238302bd43a6821b7d643c49f05` on `feat/issue-181-typed-cleanup-importer` from parent `b65a0f7`; the importer, direct workflow/test consumers, ownership and derived lock belong to this one rollback boundary. This passive SHA record changes no executable path (runtime harness N/A); rollback of this evidence-only line does not revert the typed work unit. Hosted checks and PR delivery must be read back at the final pushed head before they count.
- Draft child [PR #216](https://github.com/eff3ct0/agent-foundry/pull/216) targets `feat/issue-181-integrator` at `b65a0f748308afa511d037240a4af71abed0bc79`. Its initial pushed head `e01c89b9443a203c4a54a7d95889885e551069ad` had `type:feature` as the only `type:*` label at readback. Hosted Node 20 `verify` succeeded (run `36027588643`); an earlier PR-governance `validate` run `36027588321` failed because the label was not yet present, and the subsequent `validate` run `36027613120` succeeded after labeling. This documents both outcomes, not a claim that the failed historical run disappeared. The child stays draft, #181 stays open; later SHA-only evidence delivery must be rechecked at its own head. Rollback for this passive checkpoint is this line only.

### TM-03d local migration checkpoint (in progress)

- Branch `feat/issue-181-typed-provision-importer` was created from clean `fce70e05a125166d49fb7bb9d32f506853532f6c` without moving its parent. This source-only provision importer slice is limited to the direct workflow/test consumers, static contract, complete runtime fixture, removed ownership paths, derived payload lock, and this ledger. No source mutation, runtime or hosted verification is claimed at this pre-implementation checkpoint.
- Implemented `scripts/typed/resource-provision-and-proof.mts` and committed its compiler-derived `scripts/typed-runtime/resource-provision-and-proof.js` (the isolated emitter rewrites its sibling `.mjs` source specifier to `.js`). The old `.mjs` importer is removed; its only direct workflow/test imports now use checked-in JS. The bootstrap static checker and mutation fixture reject the old workflow import. Both new files are source-only `removed` ownership; no inherited payload entry changed. `node scripts/build-payload.mjs --write-lock` derived the combined ownership/payload lock, digest `sha256:9b97eb8859c710cf680410b431592d6641ebc0a3c9fc02398939af43e190a014`.
- Cold pre-build import of the checked-in JS and compiler-generated output comparison passed on local Node v26.9.0 / pnpm 12.4.2. Sequential checks exited 0: `pnpm build`; `pnpm typecheck`; `node --test test/resource-provision-and-proof.test.mjs test/resource-provisioning-proof.test.mjs test/resource-proof-cleanup.test.mjs test/check-bootstrap-workflow.test.mjs test/package.test.mjs` (**58/58**); `pnpm test` (**198/198**); `pnpm test:workflow-contract` (**21/21**); `pnpm test:package-consumer` (**3/3**); `node scripts/check-bootstrap-workflow.mjs` (six static checks); `node scripts/check-determinism.mjs`; `node scripts/check-factory-layout.mjs`; `node scripts/check-delivery-contract.mjs --self-check`; and `node start.mjs --self-check`. No lint/format gate is configured; no hosted Node 20 or release execution is claimed.
- Runtime harness: injected offline mutation/readback/storage fixtures cover create-once, two exact F3 reads before persistence, proof write/read, rejection/indeterminacy without retry, proof validation and persistence errors, and typed error codes. A fresh `verifyTypedRuntime` recompilation matched **all six files and every byte** (`check-real-agent-workflow.js`, `package.json`, `resource-cleanup-eligibility.js`, `resource-proof-cleanup.js`, `resource-provision-and-proof.js`, `resource-provisioning-proof.js`); no `.mjs` output was written. Staged `checkSourceModulePolicy(process.cwd(), "/usr/bin/git")` yielded **66 expected sorted diagnostics: 61 tracked `.mjs`, five payload `.mjs`, zero typed-runtime JS**. This is not a whole-tree policy pass.
- Rollback boundary: revert only the provision importer source/runtime pair and restore its former `.mjs`, the workflow and focused test imports, the bootstrap static assertion/mutation, exact runtime fixture, source-only ownership entries, builder-derived payload lock, and this TM-03d checkpoint. Preserve TM-03b/c leaves and cleanup importer, provenance verifier/policy, inherited payload/startup, and #184. No commit, push, PR, tracker mutation, hosted call, final module-policy gate, or issue closure in this slice; TM-03, TM-02 final gate and #181 acceptance remain open.
- Staged review size: **10 Git diff entries** (one rename represents two physical paths), **187 additions + 34 deletions = 221 changed lines** including **102 compiler-derived JS lines**; **119 authored changed lines** excluding that generated file. No tests or docs were dropped to meet the advisory ~400-line heuristic. Both staged and unstaged `git diff --check` exited 0; no unstaged work remains.
- Independent read-only validation of this staged slice found no severe candidate-caused defect within offline scope: 14 differential scenarios matched the previous importer in outcomes, error codes, event order and persisted bytes, with at most one creation. Fresh compilation matched the exact six-file committed runtime byte-for-byte; the 62 declared payload files, tracked modes and digest matched the staged tree, and source policy reported 61 tracked plus five payload `.mjs` diagnostics (zero typed-runtime JS). A separate local Node v26.9.0 rerun passed `node --test test/resource-provision-and-proof.test.mjs test/check-bootstrap-workflow.test.mjs test/package.test.mjs` (**45/45**), all six bootstrap static checks, and staged/unstaged diff checks. This does not establish hosted Node 20, real lifecycle credentials, release readiness, or a green whole-tree module policy.
- TM-03d work-unit commit: `28c8265fe772bc4953f5bd30fb480473d8f495b2` on `feat/issue-181-typed-provision-importer`, with parent `fce70e05a125166d49fb7bb9d32f506853532f6c`. This passive SHA record has no executable boundary (runtime harness N/A); revert only this evidence line to roll back the docs follow-up, not the typed work unit. Hosted checks and PR delivery count only after readback at the final pushed head.

### TM-03e mutation lifecycle client (bounded local continuation)

- [x] Migrate only `scripts/hosted-lifecycle-mutation-client.mjs` to `scripts/typed/hosted-lifecycle-mutation-client.mts` and compiler-derived `scripts/typed-runtime/hosted-lifecycle-mutation-client.js`; keep the GET-only read client unchanged because `.github/workflows/npm-release.yml` imports it directly and #184/npm is outside this slice. Base: clean `b245f672f2de3906fbb25e3e1386c95a61a6a75b`; new local branch `feat/issue-181-typed-lifecycle-clients` without moving its parent.
- [x] Retarget the mutation client's actual bootstrap workflow, real-agent journey, and focused test imports. Keep transport injection only, request/response caps, validation before mutation, one-attempt create/delete, and rejected versus indeterminate outcomes unchanged; add no hosted calls or shim. Guard both bootstrap and cleanup imports in the existing static contract and negative fixtures.
- [x] Register both source-only paths as `removed`, expand the complete compiler runtime file-set fixture, regenerate the payload lock only from final inputs, and retain five inherited `.mjs` payload paths. Run cold runtime import before build, sequential build/typecheck/focused/full/workflow/package-consumer/bootstrap/determinism checks, fresh runtime byte/set proof, trusted-Git policy inventory and both diff checks. Record exact evidence and rollback below. TM-03, the final TM-02 gate, Node 20 hosted verification, and #181 remain open.

Local Node v26.9.0 evidence: cold direct import of the new runtime and injected offline delete returned `{ status: "deleted" }` before build; the isolated emitter generated the new runtime and `cmp` matched its committed bytes. Builder-derived payload digest: `sha256:3776a4563cf46545665a256a5e6dd83d6e20903a5922a5bbc9e387bb965a2c2c`, changing only the ownership file checksum/digest in the payload lock. Sequential `pnpm build` and `pnpm typecheck` exited 0; focused `node --test test/hosted-lifecycle-mutation-client.test.mjs test/resource-provision-and-proof.test.mjs test/resource-proof-cleanup.test.mjs test/real-agent-journey.test.mjs test/check-bootstrap-workflow.test.mjs test/package.test.mjs` passed **59/59**; `pnpm test` passed **199/199**; `pnpm test:workflow-contract` **21/21**; `pnpm test:package-consumer` **3/3**; bootstrap static check (six checks) and determinism exited 0. Fresh `verifyTypedRuntime` recompilation matched the exact seven-file runtime set (six JavaScript modules and `package.json`) byte-for-byte, without `.mjs` output. The focused mutation fixture asserts at most one transport call for each create/delete on 403, 503 and invalid response. No hosted calls, Node 20 run, or whole-tree module-policy pass are claimed; lint/format gates are not configured.

Rollback: restore only the old mutation `.mjs` and remove its new `.mts`/compiled `.js` pair; revert the two bootstrap workflow imports, real-agent journey and focused test import, bootstrap checker and mutation fixtures, exact runtime file-set fixture, removed ownership entries, builder-derived lock, and this TM-03e checkpoint. Keep the read client, inherited payload/startup, preceding typed migrations, npm-release workflow and #184 unchanged. No commit, push, PR, issue closure, release, or workflow dispatch is authorized by this checkpoint.

Staged trusted `/usr/bin/git` source-policy readback: **65 expected sorted diagnostics** (60 tracked `.mjs`, down from 61, and five inherited payload `.mjs`); zero typed-runtime JS diagnostics. Seven runtime files matched fresh compiler output. All intended paths were staged explicitly; staged and unstaged `git diff --check` exited 0 with no unstaged paths. Staged review size: **11 diff entries** (one rename covers two physical paths), 215 additions plus 30 deletions including 134 compiler-generated JS lines; **111 authored changed lines** excluding that generated file. This transitional inventory does not satisfy the final source/generated module-policy gate.

Independent pre-delivery verification of this exact staged slice compared **30** old/new offline operation/scenario outcomes, request descriptors, error codes, and one-attempt transport behavior with no severe candidate-caused regression. The focused independent rerun passed **45/45**; the bootstrap static spot check passed all six checks. Fresh compilation matched all **seven** committed runtime files byte-for-byte; all **62** payload entries and digest `sha256:3776a4563cf46545665a256a5e6dd83d6e20903a5922a5bbc9e387bb965a2c2c` matched the staged inputs. The trusted source inventory reported **60 tracked plus five payload `.mjs`** diagnostics, zero typed-runtime JS; the read client and npm-release workflow remain untouched. Pre-commit `node scripts/check-bootstrap-workflow.mjs` passed six checks; `git diff --cached --check` passed. These checks are local Node v26.9.0 evidence only, not hosted Node 20 or a passing whole-tree policy. Rollback is the TM-03e source/runtime and direct-consumer boundary above; revert this evidence paragraph alone if its verification record must be withdrawn without rolling back the migration.

TM-03e implementation work-unit commit: `4b542317d67a302dce6828cccb99505c892f946b` from parent `b245f672f2de3906fbb25e3e1386c95a61a6a75b`. This passive SHA record changes no executable boundary (runtime harness N/A); revert this line alone to withdraw its documentation without reverting the work unit. Hosted verification counts only after readback at the final pushed head.

### TM-03f artifact identity (bounded local continuation)

- [x] Migrate only `scripts/artifact-identity.mjs` to `scripts/typed/artifact-identity.mts` and compiler-derived `scripts/typed-runtime/artifact-identity.js` from clean parent `449a53787e2ed8e46f38788a41128efea27cb7d6` on local child `feat/issue-181-typed-artifact-identity`. Preserve the existing artifact envelope, digest ordering, errors, and generated-tree checks without a shim.
- [x] Retarget exactly the direct imports in `scripts/installed-runner.mjs`, `scripts/package-consumer-verify.mjs`, and `test/package.test.mjs`; keep indirect workflow commands and inherited payload/startup, test-runner globs, npm-release/#184, and hosted behavior untouched. Add focused contract assertions if needed.
- [x] Register the new source/runtime as `removed` ownership and remove the old path; extend the exact compiler runtime file-set fixture, then regenerate the ownership-derived payload lock after final inputs. Check cold JS import before build, sequential build/typecheck/focused/full/consumer/workflow/static/determinism/layout checks, complete fresh runtime file-and-byte identity, trusted `/usr/bin/git` source-policy diagnostics, and both diff checks. Record initial failures, runtime harness, review size, and rollback; keep TM-03, final TM-02 policy, and #181 open.

This local-only work unit does not authorize a commit, push, PR or issue mutation, hosted call, release, or a claim of a green whole-tree module policy. Rollback boundary: restore only the old identity `.mjs` and its three direct imports, remove the new `.mts`/compiler-derived `.js` pair, and revert the exact runtime fixture, removed ownership entries, builder-derived lock, and this TM-03f checkpoint. Preserve all preceding typed children, inherited payload/startup, workflows, and #184.

Local Node v26.9.0 / pnpm 12.4.2 evidence: cold pre-build direct import exposed exactly four identity exports; the initial compiler output matched the new checked-in JS with `cmp`. `node scripts/build-payload.mjs --write-lock` changed only the ownership checksum/size and payload digest (`sha256:e82a1ff530f38743e32805ef1cc643e5bc9a0e3d286878340e63b08b19030b8c`); the payload declaration and five inherited `.mjs` paths remain unchanged. Sequential `pnpm build` and `pnpm typecheck` exited 0. The first focused `node --test test/package.test.mjs test/installed-runner.test.mjs test/package-consumer-verify.test.mjs` failed **22/23** because the new test incorrectly expected the legacy `ArtifactIdentityError.name` to be its class name; Node retains `name: "Error"` for that subclass. The test now asserts the actual subclass instance and exact message without changing runtime behavior; rerun passed **23/23**. `pnpm test` passed **200/200**; `pnpm test:package-consumer` **3/3**; `pnpm test:workflow-contract` **21/21**; bootstrap workflow static check passed six checks; determinism and factory layout exited 0. No lint/format gate is configured.

Offline runtime harness: the packed two-install fixture preserves exact package and generated-tree identity, both source-only consumer/runner fixtures exercise the imported compiled module, and the new direct fixture checks schema v1/v2, tarball and payload digests, verified/unavailable release envelopes, invalid source/release errors, and symlink rejection without credentials or hosted calls. A fresh independent emitter run and `verifyTypedRuntime` matched **all eight** runtime files and every byte, including the ESM manifest; no emitted `.mjs`. Staged trusted `/usr/bin/git` policy readback reported **64 expected transitional diagnostics: 59 tracked `.mjs` plus five payload `.mjs`, zero runtime JS**; this is NOT a green whole-tree policy. Review diff before this evidence addendum: eight diff entries, 167 additions plus 20 deletions including 94 compiler-derived JS lines; 93 authored additions plus deletions excluding compiled JS. No tests or docs were removed to fit the 400-line advisory. Staged and unstaged `git diff --check` both exited 0. No hosted Node 20, publication, commit, push, PR, issue closure, or final TM-02 policy claim follows from this local checkpoint.

Independent read-only verification of the same staged eight-entry slice on Node v26.9.0 passed the focused `node --test test/package.test.mjs test/installed-runner.test.mjs test/package-consumer-verify.test.mjs` (**23/23**) and six bootstrap static checks. Across **24** offline old/new scenarios, success/rejection, error names and messages, and identity/digest bytes matched. Fresh compilation matched the complete **eight-file** committed runtime set byte-for-byte; the **62** declared payload records, modes, bytes and digest matched the staged inputs. Trusted-Git policy still reported **59 tracked plus five payload `.mjs`** diagnostics and zero typed-runtime JS. Neither this proof nor the writer's full **200/200**, consumer **3/3**, and workflow **21/21** local suites establishes hosted Node 20 or a green whole-tree gate. No severe candidate-caused defect was found within this offline scope. Rollback remains the exact identity source/runtime, three direct imports, runtime fixture, ownership, payload lock, tests and checkpoint boundary above; preserve the inherited payload, preceding children, workflows and #184. The earlier local-only authorization describes the preceding checkpoint; the owner's subsequent instruction authorizes only this issue-specific commit, HTTPS child push, and one draft PR. Risk: medium, under the 400-line advisory. Keep TM-03, TM-02 final enforcement and #181 open.

TM-03f implementation work-unit commit: `3a6024d7fed2a8ec7ef93471ae8b66b1230850c2` from parent `449a53787e2ed8e46f38788a41128efea27cb7d6`. This passive SHA record changes no executable boundary (runtime harness N/A); revert only this line to withdraw the follow-up without reverting the work unit. Hosted verification counts only after readback at the final pushed head.

### TM-04a generated governance self-check correction (pre-cutover, local only)

- [x] Fix only the inherited PR-governance checker's template lookup in the source and generated layouts. Starting point: clean `b562aaae72824f173d1d8c58ad5aeb9f9b6d3f5e` on new local `fix/issue-181-generated-governance-check`, without moving its `feat/issue-181-typed-artifact-identity` parent. An offline packed target already proved plan -> apply -> verify -> noop, generated startup WORK and relocated delivery/labels/layout, but generated governance `--self-check` failed because it read root `templates/pull-request.md`; creator relocates that exact template to `.factory/templates/pull-request.md`. The check now selects exactly that path only for the relocated checker, requires both templates, and rejects symlink components; no fallback, trust/validate contract change, or remote action.
- [x] Add a focused regression in the existing package test using a fresh, actual offline packed/generated target. Source and relocated self-checks pass; removing or corrupting the relocated template fails despite a valid root-level decoy, and a symlinked template fails closed. Retain all source-only governance tests. Regenerate `package/payload-manifest.json` with `node scripts/build-payload.mjs --write-lock` after final inherited script bytes change.
- [x] Run the sequential local checks and record results below. This checkbox covers TM-04a verification only, not the full TM-04 cutover or #181 acceptance.

The initial focused packed regression failed **0/1** at the expected generated `ENOENT` for `<target>/templates/pull-request.md`; after the checker correction, one test assertion initially expected the wrong Node rejection wording and was tightened to the actual missing governance marker. Final focused packed rerun and the full focused suite passed. On Node v26.9.0 / pnpm 12.4.2, sequential `pnpm build` and `pnpm typecheck` exited 0; `node --test test/package.test.mjs test/check-pr-governance.test.mjs test/startup.test.mjs` passed **32/32**; `pnpm test` **201/201**; `pnpm test:package-consumer` **3/3**; `pnpm test:workflow-contract` **21/21**; `node scripts/check-bootstrap-workflow.mjs` passed all six static checks; `node scripts/check-determinism.mjs` and source `node scripts/check-pr-governance.mjs --self-check` exited 0. The fresh packed fixture proved plan `planned`, apply `applied`, verify `verified`, rerun `noop`, generated startup `WORK`, and relocated governance `self-check OK`, plus the missing/wrong/symlink negatives. No hosted Node 20 or published package is claimed; lint/format gates are not configured.

`node scripts/build-payload.mjs --write-lock` produced digest `sha256:f988ea1a300969a58d36d5c18e11f7853056257db5bb983b21831b5cc5421f99`; a read-only builder rerun reproduced it, changing only the inherited checker size/hash and payload digest (not the 62 declared paths). Trusted `/usr/bin/git` source-policy readback returned **64 expected transitional diagnostics**: **59 tracked `.mjs` + five payload `.mjs`, zero other/typed-runtime JS**, sorted; this is NOT a final policy pass. Final local staged review: four intended files, **70 additions + 6 deletions = 76 authored lines** including the builder-derived lock, within the ~400 advisory without code-golf; no unstaged changes. Both staged and unstaged `git diff --check` exited 0; the parent ref still points to `b562aaae`. Rollback: revert only the checker template selection and component guard, its packed fixture, builder-derived manifest, and this TM-04a checkpoint. Preserve all preceding typed work, root startup, other inherited assets, governance workflow, npm/release/#184 and final policy boundary.

TM-04 remains open: this correction does not migrate `start.mjs`, inherited checker filenames, hooks, payload declarations, generated CI, or source/generated module-policy enforcement. TM-02, TM-03, TM-05, TM-06 and issue #181 also remain partial. No commit, push, PR, provider write, credentials, hosted dispatch, #184/npm/release change, or final policy gate is authorized for this local work unit.

Independent pre-delivery read-only validation of the four staged paths found no severe candidate-caused defect. The focused rerun passed **27/27**; source and actual offline packed/generated governance self-checks passed. With a valid root-level decoy, the relocated checker rejected a missing, malformed, or symlinked `.factory/templates/pull-request.md`. The six bootstrap static checks passed. All **62** payload records matched the staged source paths, modes, sizes, SHA-256 hashes, and digest `sha256:f988ea1a300969a58d36d5c18e11f7853056257db5bb983b21831b5cc5421f99`; staged and unstaged diff checks were clean. This corroborates the writer's sequential local results above, not hosted Node 20 or final whole-tree module policy. Risk is medium and under the 400-line advisory; RDD is globally off. Roll back only this checker, packed regression, derived manifest and TM-04a checkpoint; preserve the parent typed work and other inherited/release assets. The earlier no-commit/no-remote statement describes the local-only checkpoint; the owner's subsequent instruction authorizes one issue-specific work-unit commit, passive SHA-only documentation follow-up, HTTPS child push, and draft PR without merging or closing #181.

TM-04a implementation work-unit commit: `c57172dc351735efcbdaa1c4deb6839df58de467` from parent `b562aaae72824f173d1d8c58ad5aeb9f9b6d3f5e`. This passive SHA record has no executable boundary (runtime harness N/A); revert this line alone to withdraw the reference without reverting the correction. Hosted checks count only at the final pushed head.

### TM-04b inherited label synchronizer (bounded local continuation)

- [x] Migrate only `scripts/sync-github-labels.mjs` to typed `.mts` source and checked-in compiler-derived ESM `.js` in a nested `type:module` runtime scope. Preserve the exact catalog, argument-array execution, self-check and dry-run; never invoke live synchronization during verification.
- [x] Retarget its source workflow/test and generated workflow paths, creator relocation, inherited ownership and payload declaration. Keep root `start.mjs`, the other three inherited checkers, host-required `.ts` plugins, #184/npm workflow, and whole-tree module-policy gate unchanged.
- [x] Prove fresh complete compilation and exact file/mode/byte identities across tracked source, packaged payload, composed plan and generated tree before classifying the one generated JS module. Reject source, runtime, manifest, package or generated tampering, extra runtime files, wrong modes and incomplete pairs; do not grant a directory-wide JS exception.
- [x] Verify cold source/generated CLI self-check and dry-run, offline packed plan -> apply -> verify -> noop, builder lock, sequential build/typecheck/focused/full/package-consumer/workflow tests, static/layout/determinism checks, fresh compiler-byte proof, tracked `.mjs` inventory and staged/unstaged diff checks. Record exact results, review size and limits before any completion claim.

Scope boundary: new independent `feat/issue-181-inherited-labels` worktree from immutable `981d2de9fc2572bd1cef2ae59d66d338f756b1dc`; no commit, push, PR, issue mutation, hosted Node 20 result, release, live label sync, or full #181 acceptance is authorized. Acceptance requires source and actual generated runtime behavior plus fail-closed provenance, not a payload checksum alone. If this connected unit cannot fit the ~400-authored-line advisory without losing tests or documentation, stop at an explicit local checkpoint and report the honest measured minimum. Rollback: revert only this TM-04b ledger/checkpoint, the label source/runtime pair, old label entrypoint removal, its direct workflow/tests/docs, creator relocation and generated policy proof/fixtures, inherited ownership/payload entries and builder-derived lock; preserve all prior typed children, root startup and other inherited assets.

Local checkpoint (Node v26.9.0, pnpm 12.4.2; staged but uncommitted): the label workflow now installs pinned pnpm without persisting checkout credentials and runs `pnpm build` to verify compiler bytes before exposing its token to the live sync step. No live sync was executed. The source policy checks exact Git-index bytes, mode `100644`, payload identities and fresh two-file compiler output; the generated policy checks the composed plan, checkout-to-payload bytes/modes and fresh generated output. An initial focused run before packaging failed because `dist/payload-manifest.json` did not yet exist; an initial negative test then exposed that whitespace-only `.mts` changes can leave emitted JS unchanged, which the Git-index byte comparison now rejects. A packed-payload tamper assertion initially read only the subprocess error message instead of its JSON stdout; it was corrected and rerun successfully.

After final payload inputs, `node scripts/build-payload.mjs --write-lock` derived digest `sha256:578f3cd05d2f106f935d795711b81760788a363d8f2993adc62dddc8781d7825`. Sequential local `pnpm build` and `pnpm typecheck` exited 0; focused `node --test test/sync-github-labels.test.mjs test/package.test.mjs test/creator.test.mjs` passed **48/48**, `pnpm test` **202/202**, `pnpm test:package-consumer` **3/3**, and `pnpm test:workflow-contract` **21/21**. Bootstrap workflow static check (six checks), factory layout and determinism exited 0. Cold source CLI self-check returned `self-check OK` and its dry-run printed the exact ten catalog labels without `gh` calls. The real offline packed fixture passed plan `planned` -> apply `applied` -> verify `verified` -> apply `noop`, generated startup `WORK`, relocated label CLI self-check and ten-label dry-run. It compared all three source/installed-package/generated paths byte-for-byte and generated modes, freshly recompiled complete package/generated output, and rejected per-file packaged tampering; source and generated policy fixtures rejected missing files, wrong bytes/modes, and extra runtime JS. Staged source-policy readback reports **58 tracked `.mjs` and four payload `.mjs`** (each down one), with no unclassified typed-runtime JS; this is NOT the full no-`.mjs` policy gate. Hosted Node 20, a published consumer and live label synchronization remain unobserved; no lint/format script is configured. The final staged/unstaged diff checks and exact authored review size are recorded in the final checkpoint below, not inferred from this intermediate snapshot.

Final local-only checkpoint: after strengthening the workflow-order fixture to reject absent or late `pnpm build`, sequential focused tests passed **48/48**, `pnpm test` **202/202**, package-consumer **3/3**, workflow-contract **21/21**, bootstrap static (six checks), factory layout and determinism; fresh label compiler verification returned exactly `package.json` and `sync-github-labels.js`, with **58 tracked + four payload `.mjs`** diagnostics and no other JavaScript diagnostics. Final staged diff: **17 Git entries**, **281 authored additions plus deletions** excluding the **78 compiler-derived JS lines** (the rename is recognized); staged and unstaged `git diff --check` exited 0 with no unstaged files. This verifies the local slice only: TM-04, the final module-policy gate, hosted Node 20, PR delivery and issue #181 remain open. Rollback is the exact TM-04b boundary above; no remote or live label write occurred.

TM-04b implementation work-unit commit: `eac297ab00a716bc2194682ed646d7d4e7974cbe` from parent `981d2de9fc2572bd1cef2ae59d66d338f756b1dc` on `feat/issue-181-inherited-labels`. Independent local Node v26.9.0 verification of this commit reported `pnpm build` and `pnpm typecheck` exit 0; focused tests **26/26**, full tests **202/202**, creator **22/22**, and package-consumer **3/3**. The offline packed/generated fixture and source-index byte proof passed; neither implies a published consumer or a whole-tree policy pass. A parent spot check of `pnpm test:workflow-contract` passed **21/21**. The attempted literal `node --test test/workflow-contract.test.mjs` failed because that file does not exist; the actual package script targets `test/check-bootstrap-workflow.test.mjs` and passed. This docs-only SHA/evidence follow-up has no new executable runtime boundary (N/A); revert only this paragraph to withdraw it without reverting TM-04b. Whole-tree typed gating, hosted Node 20, live label synchronization and issue #181 acceptance remain pending; no remote action was performed for this follow-up.

### TM-04c inherited PR governance (bounded local continuation)

- [x] Migrate only `scripts/check-pr-governance.mjs` to typechecked `scripts/typed-inherited/check-pr-governance.mts` and compiler-derived `scripts/typed-inherited-runtime/check-pr-governance.js` under the shared ESM package. Preserve source and relocated `--self-check`, PR policy semantics, root versus `.factory/` template selection and symlink rejection, and the GitHub token boundary. The CLI does not offer `--dry-run`; do not invent one or make live requests.
- [x] Retarget actual source/generated governance workflow, direct tests and active documentation; register inherited source/runtime and payload identities and regenerate the lock with the builder only after final inputs. Do not change root `start.mjs`, delivery/layout checkers (especially #176's newer layout), host-required `.ts` integrations, #177 bindings, or #184 publish workflow.
- [x] Extend source and generated provenance from one to exactly two inherited modules: exact tracked Git-index bytes and regular modes, complete fresh compiler set/bytes, payload bytes and modes, recomposed creator plan and generated bytes/modes; reject missing/tampered/extra runtime files without a general `.js` exemption. Test source/generated tampering and a freshly packed target's governance self-check; retain expected transitional source/generated diagnostics rather than claiming a green whole-tree policy.
- [x] Run sequential verification after final lock derivation: `pnpm build`, `pnpm typecheck`, `node --test test/check-pr-governance.test.mjs test/package.test.mjs test/creator.test.mjs`, `pnpm test`, `pnpm test:creator`, `pnpm test:package-consumer`, `pnpm test:workflow-contract`, `node scripts/check-delivery-contract.mjs --self-check`, `node scripts/check-determinism.mjs`, `git diff --check`. Record observed counts, policy diagnostics and authored changed lines. Test mode: ordinary functional tests, not strict TDD.

Route: one owner-delegated bounded implementation writer on clean `feat/issue-181-inherited-governance` at `1645249d3ffecafff631dc02297516c0af705e61`, following already-published draft #225. No remote calls, live GitHub synchronization, branch or PR mutation, commit, or claim of hosted Node 20 verification. Acceptance is the coherent source/payload/generated cutover with negative provenance and actual packed CLI proof; independent verification and parent spot check are required before any commit. The ~400 authored-line budget is advisory; do not drop checks or documentation to fit it, and do not create a PR or infer `size:exception`. Rollback: restore only the old governance `.mjs`, remove its new typed source/runtime pair, revert governance consumers, the two-module inherited provenance extension and tests, inherited ownership/payload entries and builder-derived lock, and this checkpoint; retain TM-04b labels, #176 layout, #177 bindings and #184 publish changes. Issue #181 and the final whole-tree module-policy gate remain open.

Local outcome (Node v26.9.0): the source workflow verifies committed compiler output with a pinned Node/pnpm build before the only token-bearing validation step; creator removes that archetype-only build step from the generated workflow because generated projects have no compiler toolchain. The generated command is explicitly relocated. The source check proves exact staged Git bytes/modes for both source/runtime pairs and ESM manifest, while the generated check reconciles all planned, packaged and disk bytes/modes and recompiles the complete three-file ESM runtime. The builder-derived payload digest is `sha256:5f4e837651761e6c10bd2ed8c353945df935df3458d5072d1f35e94a2cd6c06f`. The actual offline packed fixture passed plan -> apply -> verify -> noop, source and generated governance `--self-check`, template missing/corrupt/symlink negatives, all five inherited source/package/generated byte identities, and packaged tamper rejection. The CLI has no safe dry-run; an attempted `--dry-run` with no token fails before any GitHub request. Disposable source/generated policy fixtures reject source/runtime/manifest byte and mode drift, missing entries and extra runtime JavaScript. No live GitHub call was made.

Sequential post-lock verification: `pnpm build` exit 0; `pnpm typecheck` exit 0; focused `node --test test/check-pr-governance.test.mjs test/package.test.mjs test/creator.test.mjs` **50/50**; `pnpm test` **202/202**; `pnpm test:creator` **22/22**; `pnpm test:package-consumer` **3/3**; `pnpm test:workflow-contract` **21/21**; delivery self-check and determinism exit 0. The source policy reports **60 expected transitional diagnostics** (57 tracked plus three payload `.mjs`, no other extensions), down from 58+4; the generated fixture asserts the three corresponding `.mjs` diagnostics. This is NOT the final whole-tree no-`.mjs` gate. Lint/format is not configured; hosted Node 20 remains pending. An independent verifier subsequently passed Node 26 build/typecheck, focused **50/50**, source and packaged-generated CLI self-checks, exact index/payload/compiler identities and staged/unstaged diff checks; this is local functional evidence, not a receipt or issue closure. Final diff/size/status evidence follows separately; the source and generated workflows, payload and lock must remain one rollback unit.

Final writer-stage snapshot: 15 Git diff entries (the renamed governance source spans two physical paths), 324 additions plus 82 deletions = **406 total changed lines**, of which 155 are exact compiler-generated JavaScript; **251 authored changed lines** including the derived lock. No work was compressed to fit the advisory budget. At that snapshot the branch was `1645249d3ffecafff631dc02297516c0af705e61`; all intended paths were staged, none unstaged. Both staged and unstaged diff checks passed. The bootstrap static checker passed all six checks, the legacy layout self-check on this #181 branch passed, the read-only payload builder reproduced the lock digest, and source governance `--self-check` passed. The newer #176 layout checker has NOT been integrated or verified here. The independent validation above was performed after this writer snapshot; parent spot check, commit, remote delivery, live synchronization and hosted Node 20 remain pending. Leave this unit at a verification checkpoint, not issue closure.

TM-04c implementation work-unit commit: `63bd1ebf6751f7236d14e292828ed0b2da80b695` from `1645249d3ffecafff631dc02297516c0af705e61` on `feat/issue-181-inherited-governance`. The preceding writer-stage paragraph is historical; parent spot focused tests passed **50/50** after the independent verifier. This passive SHA record has no new executable boundary (N/A); revert only this paragraph to withdraw the reference without reverting the behavior and tests. PR delivery, hosted Node 20 and whole-tree #181 acceptance remain pending.

### TM-04d inherited delivery contract (bounded local continuation)

- [x] Migrate only `scripts/check-delivery-contract.mjs` to typechecked `scripts/typed-inherited/check-delivery-contract.mts` and compiler-derived `scripts/typed-inherited-runtime/check-delivery-contract.js`. Preserve both CLI self-check outputs, protected approval-denial semantics, source-versus-`.factory` root resolution, and offline source/packed/generated execution without live provider calls.
- [x] Retarget active direct consumers, tests and docs (no workflow directly invoked this checker); register inherited source/runtime ownership and payload paths, then derive the payload lock only after final input bytes. Leave #176 layout, root startup, host-required `.ts`, #177 bindings and #184 publish workflow untouched.
- [x] Extend source and generated provenance from two to exactly three named inherited modules (labels, governance, delivery): complete compiler file set, exact Git index bytes/modes, payload identities, composed plan and generated disk bytes/modes. Add tamper, missing, wrong-mode and extra-file negatives without a general JavaScript exemption.
- [x] Run sequential build, typecheck, focused delivery/package/creator tests, full tests, creator, package-consumer and workflow-contract suites, determinism and diff checks after lock derivation; exercise both CLI self-checks and offline packed/generated negatives and report transitional source/generated policy diagnostics, honest authored/generated diff sizes and rollback.

Route: one bounded writer on clean `feat/issue-181-inherited-delivery` at `b6436d9c8c9f04c09e32bd2a8f8702677a0e81b0` in `/home/steam/git/agent-foundry-worktrees/issue-181-inherited-labels`; draft parent PR #227 is historical context only. RDD globally off. TDD mode: ordinary functional tests, not strict RED/GREEN. No remote/provider call, commit, push, PR, merge, child agent or edit to preceding branches is authorized. Independent parent validation and spot check precede any future commit. Advisory review budget ~400 authored additions plus deletions; never trim checks or code-golf. If the honest cohesive slice exceeds it, stop before PR and report `size:exception`/recut needed without assuming approval. Rollback unit: restore the previous delivery `.mjs` and its consumers, remove only its typed source/runtime pair, revert three-module provenance and adjacent tests, ownership/payload declarations, derived lock and this continuation; retain the preceding label/governance work and excluded boundaries. #181 and the final whole-tree policy remain open.

Local writer checkpoint (Node v26.9.0): `node scripts/build-payload.mjs --write-lock` after final payload bytes produced `sha256:95fafcf87ea2beab098031aa653b29c5830764c0f2e3842a4e6be845c4c3a814`; read-only builder reproduced it. Fresh emitter output matches the committed delivery JS byte-for-byte, and the source policy verified all four compiler outputs (three JS modules plus ESM manifest), exact staged Git source/runtime/payload bytes and modes. A real packed fixture passed plan -> apply -> verify -> noop, source and generated delivery `--self-check` and all three approval self-checks; the packed CLI's structural self-check correctly rejects missing `docs/bindings.md` because a raw payload is not an initialized project. No provider request was made. Negative fixtures reject every source/payload inherited file's untracked or changed index bytes, wrong modes, extra runtime JS, packaged payload tampering, generated byte/mode drift and missing generated delivery runtime. Initial focused run passed 47/49: its old two-module source fixture lacked the third module, and it incorrectly expected the raw packed payload to contain generated bindings. Both fixture expectations were corrected; rerun passed **49/49**.

After lock derivation, sequential `pnpm build` and `pnpm typecheck` exited 0; corrected focused **49/49**, `pnpm test` **202/202**, `pnpm test:creator` **22/22**, `pnpm test:package-consumer` **3/3**, `pnpm test:workflow-contract` **21/21**, determinism, source delivery self-check, approval self-check, bootstrap static (six checks), legacy layout, startup and staged/unstaged diff checks passed. Staged source policy still reports **56 tracked plus two payload `.mjs`** violations, no unclassified JavaScript; generated fixture expects two remaining `.mjs` paths (`.factory/scripts/check-factory-layout.mjs`, `start.mjs`). This is not a passing whole-tree policy. Lint/format is not configured; Node 20, live providers, publication and hosted checks were not run. Current Git-recognized staged diff before this checkpoint addendum: 13 entries, **425 additions + 59 deletions = 484 total lines**, including **310 exact compiler-generated JS lines**, so **174 authored additions plus deletions**; the final task checkpoint increases the authored count. One rollback unit remains the boundary stated above. No commit or remote delivery is authorized pending independent parent validation and spot check.

Independent pre-commit validation of the staged source/runtime/payload/generated bytes and modes passed with identical index identity before and after, no unstaged paths, Node 26 build/typecheck, focused **49/49**, both delivery CLI self-checks, and staged/unstaged diff checks. The initialized generated target executes the relocated checker; a raw uninitialized payload without bindings is not the generated target. Parent focused spot check passed **49/49**. This is local evidence only, not hosted Node 20, live provider approval or whole-tree policy completion. The prior writer-stage no-independent-check statement is historical; commit and delivery remain pending.
