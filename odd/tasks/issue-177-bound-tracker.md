# ODD feature task: #177 Bound task provider is canonical

## Status and immediate path

- Ticket: [#177](https://github.com/eff3ct0/agent-foundry/issues/177), open with `type:product` and `status:approved` at planning readback.
- Phase: BT-04 offline contracts and generated-project assertions verified locally; the issue-wide phase is not advanced without bound-provider confirmation. This file is a non-authoritative planning projection, not a tracker handoff or a substitute for provider confirmation.
- Delivery checkpoint (2026-09-23): three child PRs are open with clean diffs and `type:feature` as their only `type:*` label; GitHub PR-governance `validate` was successful at readback (details below). This is review readiness, not provider-native task confirmation or issue completion. Checks may rerun after this checkpoint.
- Current worktree: `/home/steam/git/agent-foundry-worktrees/issue-177-tests`; branch: `test/issue-177-provider-contracts`; child #3 base: `6dfc5dd` on child #2. Historical BT-03 worktree: `/home/steam/git/agent-foundry-worktrees/issue-177-bindings`; branch: `feat/issue-177-provider-bindings`. Historical BT-02 worktree/base: `/home/steam/git/agent-foundry-worktrees/issue-177` at `d6b0f68a961ab4a312f2a2dbbae332a408ff5a5d`.
- Authorization: the earlier BT-04 local-only restriction is historical. For this checkpoint the owner authorized one document-only commit and pushing **only** `test/issue-177-provider-contracts` to `eff3ct0/agent-foundry` via the existing `gh` HTTPS credential session; no SSH, other branch/PR/issue mutation, merge, or bound-provider operation is authorized.
- Next: human review and integration of child #1 into the integrator branch, then child #2 and child #3 in dependency order; a draft/no-merge tracker PR can only be opened when the tracker differs from `main`. BT-05/BT-06 provider-native confirmation and delivery closeout remain pending. Before any provider-native transition, read the bound provider first; stop if confirmation and readback are unavailable.

## Objective, problem, and why

Make the task provider selected at setup through `TASK_TRACKER`, `TRACKER`, and `TRACKER_KEY` the **only canonical durable task store** for every task/TODO mechanism required or configured by the user's harness. Today the archetype says to use the bound tracker exclusively, but it does not consistently specify operation confirmation, state readback, or how local task files and UIs remain derived. Without that distinction, a harness can report completion in a local task list while the bound provider has no confirmed task, status, comment, checkpoint, or phase handoff. Ephemeral scratch notes are not durable tasks.

This is contract groundwork complementary to #28 (provider-neutral phase state) and #115 (provider-aware setup and handoff); #178 owns adapter implementation, not #177.

## Scope and boundaries

**In scope for authorized later implementation:** clarify `AGENT.md`, `templates/agent-runbook.md`, `templates/handoff.md`, `docs/agent-init.md`, the abstract task-provider contract and all five task-provider fragments, and both template and generated `docs/bindings.md` where the generated contract requires it. Include `src/creator.ts` binding composition only if needed to preserve the selected provider and tracker identity; add focused contract/generation tests and documentation checks. Confirm local files/UIs (including `odd/*.md`) are optional derived projections of provider-confirmed state; never require one or treat one as a fallback. Preserve the existing phase model and protected approval gate.

**Excluded:** provider adapters or operation implementation (#178), changing local projection formats, remote credentials, provider APIs, unrelated provider-neutral phase behavior (#28), setup/agent-handoff implementation (#115), repository rebrand, hosted provider mutations, and any assumption that GitHub is canonical for non-GitHub selections. No source change is part of this preparation delegation.

**Authorization and safety:** no remote operation in this preparation step. The original authorization permits later delivery on the named repository through the authorized `gh` session, subject to approval gates; it does not imply permission for unrelated provider mutations or hosted journeys. Provider-specific tests use offline/injected fixtures unless the exact remote operation, destination, and credential are authorized. Do not treat this temporary preparation boundary as revoking the original delivery authorization.

## Acceptance contract (from #177)

- [ ] AC1: `AGENT.md`, `templates/agent-runbook.md`, `templates/handoff.md`, `docs/agent-init.md`, and the task-provider contract require **every** durable task/TODO mechanism configured or required for the user's harness to persist durable state to the setup-bound provider.
- [ ] AC2: Local files and task UIs, if used, are optional and non-authoritative projections of provider-confirmed state (including `odd/*.md`); no local projection is mandatory or an accepted fallback.
- [ ] AC3: Durable create, update, checkpoint, and completion count as successful only after bound-provider confirmation **and readback of the intended state**; cold resume reads provider state first. Apply the same rule to status transitions, comments, and phase handoffs.
- [ ] AC4: The invariant holds for `jira`, `github-issues`, `github-projects`, `linear`, and `custom` without conventional mapping of any provider to GitHub.
- [ ] AC5: Failure, unsupported operation, ambiguous identity, or failed/mismatched readback blocks work and records the exact bound-provider operation needed to resume; no silent local fallback or false completion.
- [ ] AC6: Generated downstream docs preserve both selected provider and tracker/board identity as the source-of-truth binding.

## Task route and trigger evidence

One writer per work unit. Stable IDs are retained across resumption; a checkmark requires recorded evidence, not just prose. Routes describe planned local execution, not permission to delegate or write remotely now.

| ID | Task and finish condition | Route / trigger evidence |
| --- | --- | --- |
| BT-01 | [x] Capture the approved issue, scope, current contract, runner, risks, and next step in this file before source edits. | Inline planning: one local artifact under the earlier planning-only worker scope. Issue readback and CodeGraph/contract reads completed. That scope did not revoke the original delivery authorization. |
| BT-02 | [x] Specify provider-neutral durability, projection, confirmation/readback, cold resume, and fail-closed rules in the five required documents and `providers/task/_contract.md`; preserve phase/approval semantics. | Direct local contract work: five required contracts updated, focused offline contract test and full runner passed; no adapter calls or provider mutation. Evidence below. |
| BT-03 | [x] Align each of the five `providers/task/*.md` fragments and generated binding composition with the bound `TASK_TRACKER` plus `TRACKER`/`TRACKER_KEY`; do not assume a GitHub task target for Jira, Linear, or custom. | Direct local composition work: creator `composeBindings` renders provider fragments into generated `docs/bindings.md`; all five choices applied, asserted and verified offline. Commit/evidence below. |
| BT-04 | [x] Add focused offline contract-checker and creator generation assertions for all five choices, confirmation/readback failure and local-projection wording, plus provider/tracker identity readback; retain existing checks. | Direct local tests: five-value contract mutation and generated-output matrix passed. Malformed readback and non-GitHub PR template fallbacks were discovered #177 contract defects and fixed with regressions. No live provider adapter or remote mutation. Evidence below. |
| BT-05 | [ ] Run the exact applicable commands and generated-output review below; record commands/results, runtime harness scenario or justified N/A, rollback boundary, and unresolved gaps. | Inline verification: #177 names a post-#112 Node verification path; applicable source and generated-doc checks precede any closeout. |
| BT-06 | [ ] Reassess authored changed-line count; choose delivery strategy with the user if risk materializes; record provider-confirmed phase handoff and delivery evidence before any `DONE`. | Historical preparation route: >400-line risk invoked `ask-on-risk`; no commit, PR, or tracker mutation occurred in that preparation step. The owner has since selected the feature-branch chain below; provider-confirmed handoff and delivery remain open. |

BT-02, BT-03, and BT-04 are committed local work units; the BT-04 work-unit commit is the child #3 review candidate, not its local checkbox. Do not split tests from behavior. Issue-wide closeout still needs provider-confirmed tracker readback, subject to the existing authorization and approval gates.

## Dependencies and decisions to resolve

- `docs/bindings.md` in this source archetype is **not bound yet**. The creator combines `src/creator.ts`'s binding header with the selected `providers/task/*.md` fragment; tests must check the resulting project, not just the unbound template.
- BT-03 resolves the varying fragment detail: a GitHub Projects draft item without a linked issue needs a confirmed provider-native comment/handoff and item readback, or blocks with the missing operation and identity; custom rules without native operations/readback also block. Neither maps to GitHub Issues or local files. These are documented contracts, not implemented provider adapters.
- BT-03 corrected Jira's former contradictory “Do not leave state only in Jira” line without introducing a second tracker.
- `MAINTAINERS.md` requires every newly tracked path to be registered in `archetype-ownership.json`; `odd/` is already removed from generated projects. Historical preparation note: that planning-only write did **not** edit the inventory. BT-02 subsequently registered this file as governance text under the removed `odd/` boundary (see evidence below); this plan update does not change that inventory.
- #178 is the adapter boundary; #179 harness wiring and #180 matrix consume this contract. Do not claim this document proves runtime persistence.

## TDD mode, runner, and checks

**TDD mode: ordinary functional tests, not strict RED/GREEN TDD.** Source: the owner explicitly selected this mode for #177 in the current request. `docs/engineering-handbook.md` still declares `TDD policy: <TDD_POLICY>`; this explicit task choice resolves this plan's testing mode without claiming the project-wide placeholder is filled. Keep the `TESTING/TDD` phase and runnable tests; do not require a strict RED/GREEN sequence for this task.

**Runner (owner-selected):** `pnpm test` + `pnpm test:creator` for ordinary functional tests. This is a post-#112 Node creator project, `pnpm@12.4.2` (declared in `package.json`); both commands use `node --test` after `pnpm build`. Contract checker: `node scripts/check-delivery-contract.mjs --self-check`. No Python initializer command is valid closure evidence after #112. `node start.mjs --self-check` tests startup routing only, not initialization.

Preparation-time verification plan (retained as historical planning evidence; BT-02 results appear below):

- [ ] `pnpm typecheck`
- [ ] `pnpm test` (owner-selected functional test runner; record result)
- [ ] `pnpm test:creator` (owner-selected creator functional test runner; record result)
- [ ] `node scripts/check-delivery-contract.mjs --self-check`
- [ ] If the protected approval contract is affected: `node scripts/check-delivery-contract.mjs --approval-self-check` (do not conflate this conditional gate with the task contract).
- [ ] Exercise the contract checker against **every** `TASK_TRACKER` value: `jira`, `github-issues`, `github-projects`, `linear`, `custom`; inspect each generated `docs/bindings.md` and verify the selected provider and tracker identity are authoritative. Use offline fixtures, including failure/ambiguous/readback scenarios where checkable without adapters.
- [ ] Review generated `AGENT.md`, `docs/bindings.md`, and runbook for local-only persistence, GitHub-default language, or required local projections; inspect handoff and agent-init coherence.
- [ ] `git diff --check`, ownership/payload integrity and repository Definition of Done when delivery is authorized; record exact results, runtime harness N/A for contract-only offline checks or an actual authorized scenario, and rollback boundary.

The pre-cutover Python self-check clause in #177 is conditional on a pre-#112 revision and does **not** apply at this `d6b0f68` baseline. `python3 init.py --check` is never closure evidence here.

## Review workload and delivery strategy

Pre-implementation forecast: roughly **430–600 authored additions plus deletions**: core instructions/abstract contract 110–160, provider fragments and generated identity 120–180, tests/fixtures 160–210, task/ownership and coherence 40–50. Estimates exclude generated goldens from the authored count but not from identity/receipt checks. Risk: **high**, potentially above the 400-line review budget; actual count must be measured rather than compressed by removing tests, comments, or documentation.

Historical preparation decision: **`ask-on-risk`** was the unresolved strategy at that time; the preparation step created no PR or commit. The owner has since explicitly selected **Feature Branch Chain (`feature-branch-chain`)**, not stacked merges to main and not a `size:exception`. Keep tests/docs with each behavior unit; measure each child's authored additions plus deletions against its immediate PR base, not the accumulated tracker branch. Do not compress or discard evidence to meet the 400-line review budget. If one honest split cannot keep a cohesive child within budget, stop and report the overage and `size:exception` recommendation.

Chain (original local branch plan; the later delivered PR readback is recorded below; none merged):

```text
main (eventual tracker PR base)
└── feat/issue-177-integrator (draft/no-merge tracker; locally based on d6b0f68)
    └── feat/issue-177-bound-tracker (child PR #1 → integrator; BT-02 contracts, tests, governance and this plan)
        └── feat/issue-177-provider-bindings (child PR #2 → child #1; BT-03 fragments/binding composition and matching tests)
            └── 📍 test/issue-177-provider-contracts (child PR #3 → child #2; BT-04 offline assertions and bounded contract fixes)
                └── later child branches/PRs → immediate predecessor (BT-05/BT-06 delivery evidence)
```

At planning time, the tracker PR was intended to target `main` as draft/no-merge while child PRs were reviewed and integrated in dependency order; the tracker branch still equals `main` at the later readback, so no empty tracker PR exists. Only the tracker goes to `main`, and only after user-controlled child merges and explicit user-controlled tracker merge. Each child PR must identify its start/end, predecessor, follow-up, out-of-scope work, independent verification, rollback boundary, and a diagram marking itself `📍`; retarget or rebase a polluted diff rather than mixing work units. Child #1 begins at `d6b0f68` and contains BT-02 commit `ddd9b95` plus the chain-plan commit `03a23e3`. BT-03 and later work are not in child #1.

Local assessment at the BT-02 boundary: review tier **medium**, `review_due_reason: under_budget` (native RDD off); the `d6b0f68...ddd9b95` child #1 diff measured **241 authored changed lines** (201 additions + 40 deletions, including the existing task document). Including this plan update, the local child #1 diff against `d6b0f68` measures **261 authored changed lines** before commit. This is a local assessment, not a review approval or provider-confirmed handoff. Recount the complete child #1 diff after commit and before any PR; limit is 400 authored additions + deletions.

## Progress, verification evidence, and next step

- Historical preparation readback: issue #177 open and approved in `eff3ct0/agent-foundry`; `node start.mjs` reported SELF mode; worktree branch and base SHA verified. CodeGraph identified `composeBindings` and delivery/governance checks; Markdown contracts were read directly because CodeGraph did not index them.
- Historical preparation changes: this document only. No source write, test run, generated-project run, tracker state transition, remote mutation, or commit occurred **in that preparation step**. Its verification checkboxes remain open for issue-wide closure; BT-02 evidence follows.
- Historical preparation next step: at that time, read the bound provider first and start BT-02 under the original delivery authorization and the owner's ordinary-functional-test choice. For a project-only/draft card or custom provider without a confirmed native comment/handoff and readback path, stop with the exact missing provider-native operation and identity; do not substitute GitHub or local state. At every later phase boundary, confirm the bound provider operation and read back its state before treating any local projection as current.

### BT-02 local work-unit evidence (later than the preparation entries above)

- Outcome: five required contracts now require exclusive setup-bound task persistence for every harness task/TODO mechanism; provider confirmation and intended-state readback for create/update/status/comment/checkpoint/handoff/complete; provider-first cold resume; optional derived local projections; actionable fail-closed continuation on unsupported/error/ambiguous/mismatched operations. Phase order and protected approval gate remain unchanged. Provider fragments and generated binding identity remain BT-03/BT-04 work, so AC1–AC6 remain issue-wide open.
- Focused check: `node --test test/check-delivery-contract.test.mjs` passed (7/7) after correcting a test placeholder assertion; the new regression checks the five contracts. Initial 6/7 failure was test syntax (`TASK_TRACKER` is rendered as a code literal in two contracts), not a product failure.
- `pnpm typecheck`: passed (`tsc --noEmit`). `pnpm test`: passed (174/174). `pnpm test:creator`: passed (21/21). `node scripts/check-delivery-contract.mjs --self-check`: `self-check OK`. `node scripts/check-delivery-contract.mjs --approval-self-check`: `approval self-check OK`. `node scripts/check-determinism.mjs`: `determinism and Python-removal audit OK`. `git diff --check`: passed (no output).
- Payload/ownership: registered this task file as archetype governance text under the existing removed `odd/` boundary. `node scripts/build-payload.mjs --write-lock` rebuilt the checked-in manifest; both test runners rebuilt and verified the payload without digest mismatch.
- Runtime harness: N/A; this unit changes offline contract text and a static test, not provider adapters or a live provider interaction. No remote task operation, issue update, or provider-confirmed phase handoff occurred. The local checkbox and Engram mirror do not constitute bound-provider completion.
- Rollback boundary: revert only BT-02 changes to `AGENT.md`, `templates/agent-runbook.md`, `templates/handoff.md`, `docs/agent-init.md`, `providers/task/_contract.md`, `test/check-delivery-contract.test.mjs`, `archetype-ownership.json`, `package/payload-manifest.json`, and this task entry. No BT-03–BT-06 implementation was performed.
- Next action: BT-03 must align the five selectable task fragments and generated binding identity; confirm and read back provider-native state before claiming any issue-wide phase transition or handoff.
- Commit: `ddd9b95 docs(task): require bound-provider readback for durable tasks (#177)`; this is the BT-02 work-unit boundary, not issue-wide completion.

### Chain-plan local work-unit evidence (after BT-02)

- Current scope: this feature document only; no generated project content changes. Focused check: structural readback of the chain, historical annotations, existing task/checklist preservation, and full Engram mirror; `git diff --check` passed (no output). Runtime harness: N/A for a passive local plan update; BT-02's exact test results are recorded above, not rerun as evidence for this edit.
- Rollback boundary: revert only this chain-plan document commit to restore the previous local plan; do not revert `ddd9b95` or change provider/phase state. No push, PR, issue comment, provider operation, or merge occurred in this work unit. The local plan and its mirror are not a provider-confirmed checkpoint.

### BT-03 local work-unit evidence (child #2, after `03a23e3`)

- Commit: `2676bc3e1d1abf88ded590cd8596d2574f4e8ea3` (`feat(bindings): enforce bound task provider readback (#177)`). Child #2 compares to immediate predecessor `03a23e3`: 152 additions + 31 deletions = **183 authored changed lines**, under the 400-line budget; assessment **medium**, `review_due_reason: under_budget` (native RDD off). This is local review sizing, not an approval.
- Outcome: all five fragments require provider-native confirmation and intended-state readback, provider-first cold resume, optional derived local projections, and actionable stops. GitHub Projects draft items cannot assume a linked issue or fall back to GitHub Issues; custom rules with no native operation/readback path block. The contradictory Jira line is corrected. `composeBindings` records selected `TASK_TRACKER`, `TRACKER`, and `TRACKER_KEY` explicitly in generated `docs/bindings.md`, with an actionable missing-board warning. Protected approval semantics are unchanged.
- Focused and simulated creator scenario: `pnpm build && node --test test/creator.test.mjs`: 22/22 passed. The test applies and verifies offline generated projects for all five selected providers, asserts their generated binding identity and behavior plus generated `AGENT.md`/runbook identity, and exercises missing board identity; no hosted provider operation occurred.
- Required checks: `pnpm typecheck` passed (`tsc --noEmit`); `pnpm test` passed (175/175); `pnpm test:creator` passed (22/22); `node scripts/check-delivery-contract.mjs --self-check` printed `self-check OK`; `node scripts/check-determinism.mjs` printed `determinism and Python-removal audit OK`; `git diff --check` passed (no output). `node scripts/build-payload.mjs --write-lock` regenerated the payload manifest before verification. Early focused-test failures were assertions about Markdown line wrapping and generated runbook relocation, corrected before the passing final run.
- Rollback boundary: revert only `2676bc3e1d1abf88ded590cd8596d2574f4e8ea3` (five task fragments, source and generated binding docs, `src/creator.ts`, creator test, and payload lock); do not revert BT-02 or the child #1 plan. This post-commit local task projection and its mirror are not part of that commit or a bound-provider handoff.
- Delivery at this earlier local checkpoint: child #2 remained local. No push, PR, issue comment, approval-label operation, provider-native state transition, or provider-confirmed handoff had occurred; BT-04–BT-06 and issue-wide ACs remained open. A failed or unavailable native operation needs its exact target and readback evidence before any phase advance. Later PR delivery is recorded below.

### BT-04 local work-unit checkpoint (child #3, after `6dfc5dd`)

- Outcome: the offline contract checker rejects missing task identity, mandatory durability, cold resume, native confirmation/readback, optional projection, unsupported/failed/ambiguous/unavailable/malformed/mismatched readback, and missing actionable continuation in all five provider fragments. Generated-project assertions exercise all five selections with explicit `TASK_TRACKER`/`TRACKER`/`TRACKER_KEY` readback, no mandatory `odd/` projection, and generated `AGENT.md`, bindings, runbook, handoff, agent-init, and both PR templates. The discovered #177 defects were unclassified malformed readback and retained GitHub closing references in non-GitHub generated PR templates; both now fail closed by contract/composition and have tests. No provider adapter was implemented or exercised; protected approval semantics were not changed.
- Focused offline checks: `node --test test/check-delivery-contract.test.mjs`: 8/8 passed; `pnpm build && node --test test/creator.test.mjs`: 22/22 passed. The latter applies and verifies five temporary generated projects; it is a creator simulation, not task-provider runtime.
- Required checks after final source edits and payload-lock regeneration: `pnpm typecheck`: passed (`tsc --noEmit`); `pnpm test`: 176/176 passed; `pnpm test:creator`: 22/22 passed; `node scripts/check-delivery-contract.mjs --self-check`: `self-check OK`; `node scripts/check-determinism.mjs`: `determinism and Python-removal audit OK`; `git diff --check`: passed (no output). `node scripts/build-payload.mjs --write-lock` regenerated the checked-in manifest. Earlier focused failures reflected test fixture wording/Markdown rendering and revealed the PR-template composition defect; all were corrected before these final checks.
- Runtime harness: N/A for task-provider persistence; #178 owns native adapters. No hosted task create/update/readback, issue comment, approval-label change, or provider-confirmed phase handoff occurred. Offline generated-project `apply`/`verify` does not prove provider-native durability.
- Rollback boundary: revert only this child #3 work unit (task contracts and five fragments' malformed-readback wording, PR template reference composition in `src/creator.ts`, checker/creator tests and checker, payload lock, and this checkpoint); preserve BT-02, BT-03, approval policy, and provider state. Next: BT-05 delivery/evidence review and BT-06 provider-native phase handoff only with explicit authorized operation and readback; do not treat this local checkbox as issue-wide completion.
- Child #3 work-unit commit: `20c63f4b79ae92b2768d97bfd695d381de51ff6f` (`test(task): verify bound provider contracts offline (#177)`). Against immediate predecessor `6dfc5dd`: 160 additions + 47 deletions = **207 authored changed lines**, under the 400-line budget; local assessment **medium**, `review_due_reason: under_budget` (native RDD off). This SHA is local review evidence, not approval or a provider-confirmed checkpoint. At this earlier local checkpoint no push, PR, remote mutation, or merge had occurred for this child.

### 2026-09-23 child-chain delivery readback (after the local checkpoints)

- Child #1: [PR #185](https://github.com/eff3ct0/agent-foundry/pull/185), `feat/issue-177-bound-tracker` → `feat/issue-177-integrator`, head `03a23e301a76e4a9400cbee88b4ba47c994af93a`. Boundary: `ddd9b95` BT-02 contracts/tests/governance and `03a23e3` chain-plan docs; rollback only this child from the integrator, preserving the `d6b0f68` base. Previously measured 261 authored changed lines against the integrator.
- Child #2: [PR #186](https://github.com/eff3ct0/agent-foundry/pull/186), `feat/issue-177-provider-bindings` → child #1, head `6dfc5dd133d67594c8ce7d4b6f3a292f258b4239`. Boundary: `2676bc3` BT-03 provider fragments/creator/tests/payload and `6dfc5dd` evidence docs; rollback only this child, preserving BT-02. Previously measured 212 authored changed lines against child #1.
- Child #3: [PR #187](https://github.com/eff3ct0/agent-foundry/pull/187), `test/issue-177-provider-contracts` → child #2, head at readback `d9ebb25a3042420033221c928c721e09b80379d9`. Boundary: `20c63f4` BT-04 offline contracts/tests/fixes and `d9ebb25` local evidence docs; this passive delivery checkpoint is a separate document-only commit on the same child. Roll back only the child #3 work unit and its evidence from child #2, not BT-02/BT-03. Measured 207 authored changed lines against `6dfc5dd` before this checkpoint; including this edit, the child #3 worktree diff against `6dfc5dd` is 222 authored changed lines (still below 400), subject to final committed-diff readback.
- At the 2026-09-23 GitHub readback, all three PRs were `OPEN`, `MERGEABLE`, and had exactly one `type:*` label (`type:feature`); their PR-governance `validate` check runs were `COMPLETED` with `SUCCESS`. These are point-in-time results, not a guarantee for later commits or reruns. The remote tracker branch `feat/issue-177-integrator` and `main` both resolved to `d6b0f68a961ab4a312f2a2dbbae332a408ff5a5d`; the tracker has no PR because its diff is empty. The child #3 remote SHA matched the pre-checkpoint local SHA at readback. No child has been merged.
- This checkpoint changes only `odd/tasks/issue-177-bound-tracker.md`. Structural readback and `git diff --check` are its verification; runtime harness: N/A for passive documentation. The mirror in Engram is a derived record, not bound-provider confirmation. Pending: current-commit/push SHA readback, human review and integration of child #1, later dependent reviews, and BT-05/BT-06 provider-native confirmation/handoff; AC1–AC6 and the preparation-time closeout checklist stay unchecked until issue-wide proof exists. No issue comment, label mutation, provider operation, merge, or release is part of this checkpoint.

### Local #183/main integration checkpoint (offline verified)

- In `/home/steam/git/agent-foundry-worktrees/issue-177-tests` on `feat/issue-177-integrator`, integrate the locally available `refs/remotes/authorized/main-for-177` at `5fe1a85ba31caa091c4d94c9baca22b6b9cbc381` into #177 head `b386f202f3b309f8414ed3fd99786553f5e12a14` with `git merge --no-commit --no-ff`. Preserve `MERGE_HEAD` for the authorized parent commit; do not commit, push, or merge the tracker PR into main in this work unit.
- The sole content conflict was the builder-derived `package/payload-manifest.json`. Resolve it from the combined source inputs with `node scripts/build-payload.mjs --write-lock`, not by choosing either parent's manifest. The regenerated payload digest is `sha256:a8df63b7e297b159b5c83b3ccde7f6ee604c9624ab23f7ac984a6c80b1841068`; the lock and this checkpoint are the only intended local conflict-resolution edits.
- Ordered local checks passed: `node start.mjs` (SELF mode, new repository name); `pnpm build`; `pnpm typecheck`; `node --test test/check-delivery-contract.test.mjs test/creator.test.mjs` (30/30); `pnpm test` (183/183); `pnpm test:creator` (22/22); `pnpm test:package-consumer` (3/3); delivery-contract self-check and approval-self-check (`OK` each); bootstrap-workflow checker (five static checks OK); and determinism checker (`OK`). Staged and unstaged `git diff --check` passed. No hosted journey, registry verification, or live task-provider operation ran.
- Offline generated-project tests applied and verified all five selected task providers and their `TASK_TRACKER`/`TRACKER`/`TRACKER_KEY` bindings; non-GitHub PR templates retained native references without GitHub closing references. The merged branding is covered by startup, installer, and journey workflow tests. Runtime provider durability remains unverified; this local checkpoint is not a bound-provider handoff, BT-05/BT-06 closeout, or issue completion.
- Rollback boundary: discard only the uncommitted integration of `5fe1a85` into `b386f202` (including this checkpoint and the regenerated payload lock), preserving #177's pre-merge commits and any provider state; the parent must decide how to remove a pending merge, without treating this as permission to reset here. `MERGE_HEAD` remains for that decision; do not commit, push, mutate a PR/issue, or merge the tracker PR into main in this work unit.
- Parent integration commit `1ae26e3eab5960aa63e5c4feb882d60c45dd7cef` has first parent #177 `b386f202f3b309f8414ed3fd99786553f5e12a14` and second parent verified #183 main `5fe1a85ba31caa091c4d94c9baca22b6b9cbc381`; its tree matched the fully staged verified merge and the worktree was clean. Independent high-risk read-only validation reconstructed the 62-file payload and digest `sha256:a8df63b7e297b159b5c83b3ccde7f6ee604c9624ab23f7ac984a6c80b1841068`, passed focused 30/30 and delivery self-check, and found no severe integration issue. Native committed-range assessment is `high` from inherited #183 shell workflow; RDD is off and no native review receipt exists. This is branch reconciliation, not newly authored #177 provider-adapter work. Next: deliver this integrator head, then reconcile dependent #178 without claiming BT-05/BT-06 or provider-confirmed handoff complete.
