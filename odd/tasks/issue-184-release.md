# Issue #184: Align npm and GitHub releases with automated changelogs

## Status and immediate path

- Ticket: [#184](https://github.com/eff3ct0/agent-foundry/issues/184), `OPEN`, `type:product`, `status:approved` at planning readback on 2026-09-23.
- Phase: R184-02a offline implementation and independent verification complete; R184-02b1 event-SHA guard, R184-02b2 toolchain correction, and R184-02b3 read-only npm version probe verified offline on children #2–4. R184-02b4 offline publish-attempt claim contract foundation is child #5, based on child #4 `96d31f1d1d43584d3c89e2014490c3cf72c28dce`; it is not wired into publication. R184-02b5 post-publication policy validator is an offline child #6 checkpoint, NOT an npm signature verifier or publish gate. R184-02b independent registry provenance and a fully integrated publish/skip gate remain unresolved. The overall #184 release remains open and unpublished.
- R184-02a repairs three existing preflight/readback defects with negative regressions. It can precede #181 integration; reconcile its `.mjs` scripts and tests with #181 when that migration lands. #181 typed-module integration and #183 owner-confirmed repository coordinates remain gates for final release verification and publication. Do not reset history, create a tag or Release, publish npm, or disable Template mode in this work unit.

## Objective, reason, and boundaries

Make the first `@eff3ct/agent-foundry@0.1.0` publication traceable to exactly one final verified `main` commit: `v0.1.0` tag, GitHub Release, npm version, local tarball, registry tarball, payload, and immutable evidence must agree. Automate deterministic release notes/changelog generation and gated GitHub Release creation without letting a draft or dry run publish npm. The existing `release.published` workflow currently publishes the one matching tarball once; it does not create a GitHub Release. Historical tags already claim `v0.1.0`–`v0.2.4`, while the public npm package and `0.1.0` endpoint currently return 404.

In scope: full baseline audit and recheck; owner-confirmed version/history policy; offline changelog and release-creation automation; exact source/package/readback contracts, failure tests, dry-run, package consumer checks and hosted journey evidence; narrowly staged history correction and first publication only after the relevant gates pass. Keep release-only source assets classified as `removed` in `archetype-ownership.json` when they are introduced.

Out of scope: npm unpublish, changing the package name or `foundry` command, broad branding/slug changes (#183), unrelated provider behavior, reading secrets, unreviewed GitHub metadata wording, automatic Template-mode disablement, or using mutable `main` as a substitute for a resolved tag SHA. Do not infer approval to change description, topics, homepage, or slug from #184. Do not perform the owner-reserved final Template-mode change without its own authorization.

### Exact owner-confirmed history policy (conditional, NOT executed)

The owner has expressly authorized this **specific** reset only after implementation and all pre-publication verification gates are green: retire **only** GitHub Releases `v0.2.0`, `v0.2.1`, `v0.2.2`, `v0.2.3`, `v0.2.4`; retire **only** GitHub tag refs `v0.1.0`, `v0.2.0`, `v0.2.1`, `v0.2.2`, `v0.2.3`, `v0.2.4`; then recreate `v0.1.0` at the **final verified main SHA**, create its GitHub Release with reviewed generated notes, and publish `@eff3ct/agent-foundry@0.1.0` **once**. Do not delete any other release/ref, force-move a ref, or unpublish npm. Before any destructive step, capture the **full then-live** tag and release inventory, resolved commits, notes and metadata and compare it unchanged to the approved baseline below; a new/changed/missing object or npm version stops for owner re-review. An approval label is not a substitute for that comparison. Post-publication consumer and hosted checks are necessarily subsequent to the publish, not prerequisites that can be faked by an offline fixture.

## Observed live audit (2026-09-23; read-only; not a future authorization snapshot)

Source: authenticated GitHub `eff3ct0/agent-foundry` tag refs, annotated-tag object, paginated Releases and branch/repository readback; unauthenticated public npm registry package and `/0.1.0` endpoints. `main = d6b0f68a961ab4a312f2a2dbbae332a408ff5a5d`; `isTemplate = true`; default branch `main`. The tag names and release names are **unchanged** from the last observed `v0.1.0`–`v0.2.4` / `v0.2.0`–`v0.2.4` inventory. The current repository description still mentions the old Factory OS/init.py wording; #183 owns its owner-reviewed replacement.

| Tag ref | Object type and SHA | Resolved commit SHA | GitHub Release |
| --- | --- | --- | --- |
| `v0.1.0` | annotated tag `29b2ead1901adb43abde1e398a756c895b54117a` | `d9d3592a95fef2022b0b43fcaeac940b44a4b706` | None; annotated tag message: `Archetype v0.1.0 — initial baseline` |
| `v0.2.0` | commit `bb699cbef5d72a107ab56059077f5a7223289387` | `bb699cbef5d72a107ab56059077f5a7223289387` | Published |
| `v0.2.1` | commit `3503ab89f1b017ee80e0ddf8164e7ff69d292d7d` | `3503ab89f1b017ee80e0ddf8164e7ff69d292d7d` | Published |
| `v0.2.2` | commit `787b68fde3153bd334e1e48d25abc9542e954ab0` | `787b68fde3153bd334e1e48d25abc9542e954ab0` | Published |
| `v0.2.3` | commit `d0b6bf103e382f3f5c809eb19fa024332922f432` | `d0b6bf103e382f3f5c809eb19fa024332922f432` | Published |
| `v0.2.4` | commit `a8984466f3eea0838324a204fae525ea09cefe8b` | `a8984466f3eea0838324a204fae525ea09cefe8b` | Published |

All five Releases have `draft=false`, `prerelease=false`, `name=<tag>`; `created_at` and `published_at` are distinct. `target_commitish=main` for `v0.2.3` and `v0.2.4` is **not** immutable source identity: their tag refs above resolve the commit.

| Release tag / ID | URL | Created UTC / published UTC | `target_commitish` | Complete release notes at audit |
| --- | --- | --- | --- | --- |
| `v0.2.0` / `390832125` | https://github.com/eff3ct0/agent-foundry/releases/tag/v0.2.0 | `2026-09-17T15:09:00Z` / `2026-09-17T15:19:17Z` | `bb699cbef5d72a107ab56059077f5a7223289387` | Release v0.2.0: enables the release bootstrap E2E workflow, including OpenAI-backed failure triage, and the manual template smoke-test path. |
| `v0.2.1` / `390861100` | https://github.com/eff3ct0/agent-foundry/releases/tag/v0.2.1 | `2026-09-17T15:54:33Z` / `2026-09-17T15:59:14Z` | `3503ab89f1b017ee80e0ddf8164e7ff69d292d7d` | This release validates the published artifact through the release bootstrap E2E workflow, including the configured OpenAI advisory triage and all current CI recipe cases. For a manual archetype smoke test, follow docs/smoke-test.md. |
| `v0.2.2` / `390900997` | https://github.com/eff3ct0/agent-foundry/releases/tag/v0.2.2 | `2026-09-17T16:55:45Z` / `2026-09-17T16:59:26Z` | `787b68fde3153bd334e1e48d25abc9542e954ab0` | `## What's Changed`\n* fix(release-e2e): request workflow permission (#77) by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/78\n* fix(release-e2e): preserve triage classification (#79) by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/80\n\n**Full Changelog**: https://github.com/eff3ct0/factory-template/compare/v0.2.1...v0.2.2 |
| `v0.2.3` / `390928466` | https://github.com/eff3ct0/agent-foundry/releases/tag/v0.2.3 | `2026-09-17T17:37:54Z` / `2026-09-17T17:42:10Z` | `main` | `## What's Changed`\n* fix(release-e2e): remove duplicate askpass cleanup (#81) by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/84\n* fix(release-e2e): link triage artifact in reports (#82) by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/85\n* fix(release-e2e): capture harness exception evidence (#83) by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/86\n\n**Full Changelog**: https://github.com/eff3ct0/factory-template/compare/v0.2.2...v0.2.3 |
| `v0.2.4` / `391049421` | https://github.com/eff3ct0/agent-foundry/releases/tag/v0.2.4 | `2026-09-17T20:41:43Z` / `2026-09-17T20:46:36Z` | `main` | `## What's Changed`\n* feat(journey): add real-agent orchestration contract by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/91\n* feat(template-e2e): harden repository lifecycle by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/93\n* feat(real-agent): add cold Codex journey by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/92\n* feat(real-agent-e2e): add journey assertions by @rafafrdz in https://github.com/eff3ct0/factory-template/pull/94\n\n**Full Changelog**: https://github.com/eff3ct0/factory-template/compare/v0.2.3...v0.2.4 |

Public npm registry readback: `https://registry.npmjs.org/@eff3ct%2fagent-foundry` returned HTTP 404 and the exact `/0.1.0` endpoint also returned HTTP 404. Thus no **publicly observable** versions or dist-tags were returned; do not assume private registry state or that 404 will remain true. Recheck before publication and stop if the package/version exists or registry readback is inconclusive.

## Ordered work units (stable IDs; unchecked means NOT executed)

One writer per task; tests and docs travel with each eventual behavior work unit. `route` names the next appropriate owner/implementer, **not** an authorization to delegate or mutate remotely in this session. Record focused test command/result, runtime boundary result or N/A, and rollback boundary per unit. R184-02a may be implemented independently of #181/#183; downstream release integration and publication may not bypass them.

| ID / depends on | Route and delegation trigger | Work and exact verification / finish condition |
| --- | --- | --- |
| **R184-01** / none; not a prerequisite for R184-02a | Release planner before final release integration; read-only only. | Persist a complete fresh read-only tag, annotated-object and Release metadata snapshot, npm versions/dist-tags status, current `main`, and Template-mode flag; compare to this table and the owner policy. `gh api --paginate 'repos/eff3ct0/agent-foundry/git/matching-refs/tags'` and `gh api --paginate 'repos/eff3ct0/agent-foundry/releases?per_page=100'` plus tag peeling and public npm readback; mismatch = blocked, not auto-repair. Repeat the full comparison at R184-06 immediately before any destructive action. |
| **R184-02a** / none; FIRST local implementation work unit | Local preflight/readback implementer; may start offline before #181 integration and without #183 coordinates or a new live audit. | Repair the three observed defects with focused **negative regression tests in the same unit**: `.github/workflows/npm-release.yml` must assert the actual local identity `release.sha` (not nonexistent root `source_sha`), with a workflow-contract test rejecting the old key; `scripts/npm-release.mjs:validateRegistryReadback` must reject a differing `registryIdentity.release.sha` even when package, payload and tarball match; `scripts/package-consumer-verify.mjs` registry mode must retain `apply` for **both** pnpm dlx and npx (currently `suffix.slice(1)` removes it), with command-argument regressions covering apply/verify/noop. Run `node --test test/npm-release.test.mjs test/package-consumer-verify.test.mjs test/check-bootstrap-workflow.test.mjs` and `node scripts/check-bootstrap-workflow.mjs` using offline fixtures/mocks, with no registry publication, hosted dispatch or live mutation. Target **<400 authored changed lines** for this cohesive unit, including tests; measure rather than compress coverage. Record result, runtime boundary or N/A and rollback before any future commit. |
| **R184-02b1** / R184-02a; child #2 offline checkpoint | Local pre-publish event-identity implementer; separate from R184-02b attestation work. | Require a full event SHA on **both** `release.published` and `workflow_dispatch` before the workflow proceeds to publish; compare it to the independently resolved tag's commit and reject a missing/malformed/different SHA, including manual runs dispatched from `main`. Keep the manual path only when dispatch ref is the exact tag commit. Test the readback guard and static pre-publish workflow wiring/order offline; no hosted run or publication. This does **not** verify npm provenance or provide an npm version-exists/idempotency gate for reruns. |
| **R184-02b2** / R184-02b1; child #3 offline checkpoint | Local release-toolchain implementer; separate from provenance and idempotency work. | Replace the broken Corepack activation in the npm release workflow only with `npm install --global pnpm@12.4.2`; require Node `v20.19.0` and pnpm `12.4.2` before package build. Static negative tests reject the old Corepack command, floating or unchecked versions, and out-of-order checks. Offline tests do not prove the hosted Node 20 runner. |
| **R184-02b3** / R184-02b2; child #4 offline checkpoint; PARTIAL release gate | Local exact-version observation implementer; no publish wiring in this unit. | Add a bounded, read-only injected npm registry transport in existing `scripts/npm-release.mjs`: classify exact-version and package-document agreement as `absent`/`present`, disagreement or failures as `unknown`. Test offline with fake transport in existing `test/npm-release.test.mjs`. A 404 observation does not reserve an attempt or prove absence at publish time; leave `.github/workflows/npm-release.yml` unchanged. A durable exclusive attempt claim and independently verified registry attestation/source identity remain separate unresolved gates before any publish/skip decision. |
| **R184-02b4** / R184-02b3; child #5 offline foundation; PARTIAL release gate | Local claim-contract implementer; no publish/skip wiring in this unit. | With injected transport only, make one bounded POST of `npm-publish-attempt` to the verified **new** Release ID, then require 201 metadata plus one unique uploaded asset from the Release-scoped list and exact bounded binary claim readback. The claim JSON contains only `tag`, `source_sha`, `package`, `version`, `run_id`. 422, 502/starter, lost responses, malformed/ambiguous/mismatched reads and timeouts block with no retry, overwrite or delete. Tests use offline fake HTTP. The caller must later prove fresh Release ID, per-tag serialization, exact npm version observation and independent attestation before any publish; an earlier claim never authorizes another publish. Keep the existing publish workflow unchanged. |
| **R184-02b5** / R184-02b4; child #6 offline policy; PARTIAL provenance gate | Local post-publication policy implementer; no audit adapter or publish wiring. | Given independently downloaded raw registry tarball bytes and a structurally trusted successful npm CLI 11.19.1 `audit signatures --json --include-attestations` result from the exact installed registry dependency, compare one verified provenance bundle's signed DSSE subject PURL/SHA512, local SHA256, SLSA v1 GitHub Actions source repository/ref/full commit, workflow parameters, builder and verified certificate SAN against explicit policy. Reject missing/ambiguous/unsupported evidence. The validator never verifies signatures or runs npm; a future adapter must prove process exit status, version, installation, exact registry/bytes and trusted JSON origin before using its policy result. |
| **R184-02b6** / R184-02b5; local offline adapter checkpoint | Local post-publication audit adapter implementer; no release workflow wiring. | In a new isolated npm project install only the exact public-registry version with pinned npm 11.19.1; require successful install and audit subprocesses, exact installed/lock identity, and bounded independent registry tarball bytes before passing the audit JSON to the pure policy. Inject offline subprocess/HTTP fixtures for negative paths. No first-publish proof, remote execution, or publication in this child. |
| **R184-02b7** / R184-02b6; abandoned precommit trial | No delivery. | A blocked-only, unwired decision module did not reduce the live publication risk; remove the uncommitted trial, retaining this history only. |
| **R184-02b8** / R184-02b7 review; temporary local workflow guard | One local writer; no publish or dispatch. | Place an unconditional failing command before `npm publish` in its own shell step, with no secret, external flag or approval dependency. Pin the exact step and reject deletion, relocation, decoys and bypasses with offline mutation tests. This guard applies only to revisions containing it; separately authorized integration is required before it protects `main`. Remove it only when a tested exclusive publish/skip/unknown gate replaces it. |
| **R184-05a** / #183 integrator reconciliation; scoped journey continuation | Local workflow/checker implementer on `fix/issue-184-scoped-journey-spec` from clean `d5f67abbcf00f800bd22b3f36f78d765414a5e42`; no publication or dispatch. | Repair only the inherited `@eff3ct/agent-foundry@<exact-version>` metadata parser in the real-agent journey. Fail closed for wrong package, malformed/non-exact version, extra separators and mismatched metadata; pin the executable workflow step with offline mutation tests and exercise its actual Node heredoc with scoped positive and negative fixtures. Preserve SHA/source identity, stages and release workflow unchanged. Record sequential verification, runtime boundary, rollback and outstanding hosted/publication evidence below. |
| **R184-05b** / R184-05a at `33efb8cb1ad2034d99af67f29a3fb045e1b44821`; journey YAML continuation | Local-only writer on `fix/issue-184-journey-yaml`; parent ref untouched. | Correct only the two over-indented `branch=$(...)` lines in `.github/workflows/real-agent-journey.yml` and guard the whole workflow's `run: |` indentation with offline negative mutations reproducing the inherited parse error. Independently parse the complete original/corrected workflow and count its jobs. Preserve the #217 scoped parser, stage/credential semantics and disabled npm publisher. Run sequential build, typecheck, focused/full/consumer tests, workflow/determinism checks, and staged/unstaged diff checks. No hosted dispatch, remote operation, commit, publication or release proof. |
| **R184-05c** / PR #213 head `2f4991824b84668283c28c9672157412866e7cc8` and exact main `4b2b90bf4028fc86d5b401318965eccf1d682291`; conflict reconciliation | One local writer on clean isolated `fix/issue-184-pr213-conflicts`; no merge commit or remote mutation. | Merge only the exact local main commit with `--no-commit --no-ff`; stop with merge state intact if conflicts extend beyond `archetype-ownership.json` and `package/payload-manifest.json`. Retain release-only provenance removals and main's typed runtime/#177 retained ports; never restore removed legacy `.mjs`. Derive the manifest from the resolved combined inputs. Audit shared auto-merges and keep unconditional publish `exit 1` before token checking and npm. Run the specified sequential offline build, focused/full/creator/consumer/workflow/determinism and diff checks, including the fake-npm publish harness. Record exact merge state, payload identity, results, rollback and over-budget review status; leave first publication and #184 acceptance open for independent validation. |
| **R184-02b** / R184-02a; NEXT release gate, not executed | Local provenance-contract implementer before any publish; reconcile with #181 when it lands. | Design and test independent verification of a source commit claim for the exact downloaded registry package using verified registry provenance/attestation and tarball identity, not a SHA copied from the expected local identity. Reject absent, malformed, untrusted, mismatched or indeterminate claims; preserve auditable exact-version evidence and block publication if the verification mechanism and fail-closed handling are not demonstrated offline. Investigate npm CLI [`npm audit signatures --json --include-attestations`](https://github.com/npm/cli/blob/latest/docs/lib/content/commands/npm-audit.md) for verification and attestation bundles; this is a research lead, **not** completed verification or proof that the source SHA is present and bound to this package. The first public registry attestation cannot be read before the first publish; the existing `release.published` workflow publishes before registry readback, so a post-publish failure cannot retroactively guard that publish. Document the remaining first-publish boundary explicitly rather than claiming pre-publication live proof. |
| **R184-02** / R184-02a, R184-02b, R184-01, #181 integration/reconciliation | Local release-contract implementer after #181 typed-module migration is integrated or reconciled; incorporate the R184-02a fixes without losing their regressions. | Specify and test `v<package.version>` / full resolved SHA / one package identity, 0.1.0 collision, absent/existing public npm version, annotated tags, malformed inputs, idempotent readback and unknown-outcome stop. Update release scripts and their tests without weakening `scripts/release-readback.mjs` or `scripts/npm-release.mjs`; `node --test test/npm-release.test.mjs test/release-readback.test.mjs test/release-ref.test.mjs` (or migrated `.mts` commands after #181). |
| **R184-03** / R184-02 | Local changelog implementer when exact commit and notes policy is fixed. | Generate deterministic notes/changelog for a specified full commit; exercise empty/duplicate/unrelated-history cases; dry-run emits reviewed output but never creates a tag/Release. Focused offline changelog fixture test + two-run byte equality and no remote writes. |
| **R184-04** / R184-02–03 | Local workflow implementer when notes generation and identity are green. | Gate automated GitHub Release creation on the approved tag/SHA and ready-to-publish transition; `release.published` must still run only one npm publish with provenance and immediate registry/payload/tarball/SHA readback. Reject mismatched SHA, version, payload, digest, existing or uncertain publication; never publish from draft or retry an indeterminate attempt. `node scripts/check-bootstrap-workflow.mjs`, `node --test test/check-bootstrap-workflow.test.mjs test/npm-release.test.mjs`, dry-run with offline mocked GitHub/npm boundaries and zero network mutations. |
| **R184-05** / R184-04, #181 and #183 coordinated | Verification owner after release work units land and #181/#183 integration is checked. | Run runner matrix below on the final candidate SHA, fresh generated archetype, exact local tarball and offline package/readback fixtures; record full results and reviewer evidence. Confirm R184-02a's workflow-key, release-SHA and both registry-consumer command regressions survive #181 migration before relying on exact-version checks. The hosted journey hardcodes `JOURNEY_TEMPLATE: eff3ct0/factory-template` in `.github/workflows/real-agent-journey.yml`; #183 must resolve active coordinates **after** owner-confirmed repository decisions before a hosted run can count. No invented metadata wording or silent workflow-coordinate swap. |
| **R184-06** / R184-05 | Owner-authorized release operator ONLY after all pre-publication checks green; this task does not trigger the route. | Immediately recapture the **full live** inventory including tag object/type/peeled SHA, Release ID/URL/name/notes/draft/prerelease/timestamps/commitish, npm public versions and Template mode; compare exactly with approved targets and record durable audit. Stop for any delta or missing readback. Preserve an off-repo audit copy before a destructive action; no history writes during planning. |
| **R184-07** / R184-06 | Owner-authorized release operator at the recorded exact-object gate. | Retire only the five named Releases, then only the six named tags, with per-object identity/readback and explicit staged checkpoints. Stop on partial/unknown deletion; do not broaden scope or blind retry. Require clean final tested `main` SHA and `package.json=0.1.0`; recreate `v0.1.0` there with readback; create reviewed GitHub Release and publish **once**. Verify event/workflow run identity and npm registry evidence; no npm unpublish. |
| **R184-08** / R184-07 | Release-validation owner after the exact version exists in the public registry. | Run `pnpm dlx --package @eff3ct/agent-foundry@0.1.0 foundry ...` and `npx --yes --package @eff3ct/agent-foundry@0.1.0 foundry ...` using fresh deterministic targets; check apply/verify/noop, identity/tree digest and final SHA. Run the **hosted real-agent journey** for exact `0.1.0` and verify report, cleanup and source identity, not just a green claim. Keep Template mode enabled. |
| **R184-09** / R184-08 | Owner only, separately authorized Template-mode cutover after all published-consumer and hosted gates green. | Confirm final repo metadata/slug wording decisions remain #183-owned, explicitly authorize any final Template-mode change, verify its readback and archive immutable release evidence. No automatic disable and no issue close before DoD and review gates pass. |

## Runner and delivery plan

- Testing mode: **ordinary functional tests, strict RED/GREEN TDD off** by the owner's explicit choice. Add runnable branch/failure tests alongside implementation; no waived testing or omitted negative-path checks.
- Baseline/focused commands (adjust filenames only after #181 lands): `pnpm build`, `pnpm typecheck`, `node --test test/npm-release.test.mjs test/release-readback.test.mjs test/release-ref.test.mjs test/check-bootstrap-workflow.test.mjs`, `node scripts/npm-release.mjs --self-check`, `node scripts/check-bootstrap-workflow.mjs`, `node scripts/check-real-agent-workflow.mjs`, `node scripts/check-determinism.mjs`, `node scripts/check-factory-layout.mjs`, `pnpm test:package-consumer`, `pnpm test:local-release-e2e`, `pnpm test`. Verify fresh generated-project ownership/type-policy checks after #181. Any unrecognized/migrated command is a task to reconcile, not permission to skip.
- Local dry-run: test release creator with offline injected API/npm fixtures; assert zero tag/Release/npm mutations and stable changelog bytes. A local tarball is not a registry publication. After publication, use `node scripts/package-consumer-verify.mjs --package @eff3ct/agent-foundry --version 0.1.0 --source-sha <FINAL_MAIN_SHA> --output <FRESH_OUTPUT_DIRECTORY> --release-file <VERIFIED_RELEASE_IDENTITY>` (confirm CLI/readback format before execution), followed by the hosted journey and artifact review. No hosted dispatch or live consumer test now.
- Forecast: approximately **650–1,050 authored changed lines** for the full #184 release effort across scripts/workflow, fixture tests, changelog/docs and ownership metadata, excluding generated outputs and this plan; high risk against the 400-line review budget. The owner selected **feature-branch-chain for #184** (separate from #181): tracker/integrator `feat/issue-184-integrator` at `d6b0f68`, child #1 `feat/issue-184-release` targets that integrator with only R184-02a. Later children target their immediate parent branch, not `main`; the tracker remains draft/no-merge until the children are integrated. The integrator ref was pushed but has no PR because its diff is empty; child #1 PR delivery is recorded below. Count authored additions and deletions against the empty integrator/main base for child #1; never shrink tests/docs to meet the budget.

### Child #1 work-unit evidence (R184-02a only)

- Boundary: fix the local identity check in `.github/workflows/npm-release.yml` and its static guard in `scripts/check-bootstrap-workflow.mjs`; reject a mismatched registry release SHA in `scripts/npm-release.mjs`; retain `apply`, `verify`, and rerun `apply` arguments for both registry launchers in `scripts/package-consumer-verify.mjs`. The three associated existing `.mjs` test files travel with these changes. No tag/Release creation, live registry call, publish or hosted dispatch is part of this unit.
- Focused tests: `node --test test/npm-release.test.mjs test/package-consumer-verify.test.mjs test/check-bootstrap-workflow.test.mjs` passed **24/24** (0 failed). `node scripts/check-bootstrap-workflow.mjs` passed all five workflow contracts.
- Required local verification: `pnpm build` passed; `pnpm typecheck` passed; `node scripts/check-determinism.mjs` passed; `pnpm test` passed **175/175**; `pnpm test:package-consumer` passed **4/4**. `git diff --check` and review-size accounting are recorded at the checkpoint below.
- Offline runtime boundary: the package-consumer test packed and installed a local tarball and exercised apply/verify/noop without a registry fetch; registry-mode command arguments for pnpm dlx and npx were verified as pure injected invocations. The exact workflow SHA assertion was executed against matching, mismatching and missing-release identity fixtures. Live npm registry/package execution: **N/A**, deliberately excluded until publication.
- Rollback: revert child #1's three script changes, workflow change, three regression test changes and this R184-02a evidence without modifying any npm artifact, GitHub ref/Release, #181 migration or #183 metadata. Existing release history stays untouched.
- Child #1 dependency: `feat/issue-184-integrator` (`d6b0f68`) → **📍 `feat/issue-184-release` / R184-02a** → future release-contract, changelog and gated publication children (not started here). Follow-up: implement R184-02b and reconcile the same regressions after #181, then R184-02 and subsequent gates; no #184 issue closure at this checkpoint.
- **Checkpoint R184-02a:** all eight requested local commands passed, including `git diff --check`; the seven modified tracked paths and this task record are the entire child #1 boundary. Measured **148 additions + 6 deletions = 154 authored changed lines** against main `d6b0f68` / the empty integrator, under the 400-line review budget. This is offline preflight proof only, not final release verification or authorization.
- First work-unit commit: `b044905d7f2e8e34b873ddfc11b9081ab9a41bb1` (`fix(release): close offline identity and consumer gaps (#184)`). This passive SHA evidence adds one documentation line; the cumulative child #1 diff is 149 additions + 6 deletions = **155 authored changed lines**.
- Delivery at parent readback: [PR #189](https://github.com/eff3ct0/agent-foundry/pull/189) `OPEN`, head `ba0b412` (`feat/issue-184-release`), base `feat/issue-184-integrator`, exactly one `type:*` label (`type:bug`); governance `validate` was `SUCCESS`. This is an open review checkpoint, not a merge, release or final CI guarantee; checks may rerun after the documentation commit.
- Independent verifier of `d6b0f68..ba0b412`: `pnpm build`, `pnpm typecheck`, 24 focused Node tests, `node scripts/check-bootstrap-workflow.mjs`, and `git diff --check` passed offline. No candidate-caused severe defect was observed within that scope. Registry consumer launchers had argument-level tests, not live published-package execution. **Unresolved finding:** `scripts/npm-release.mjs` `verify-registry` constructs `registryIdentity.release.sha` using `expected.release.sha`; `validateRegistryReadback` compares that seeded value to the same expectation. This comparison rejects mismatched injected identities but is tautological in the live path and is **not independent provenance of the published package source**. R184-02b must supply verified attestation/source-SHA evidence before any publication; neither this review nor the npm CLI investigation satisfies that gate.

### Child #2 local work-unit evidence (R184-02b1 only)

- Boundary: `.github/workflows/npm-release.yml` passes the event name and **unconditional** `github.sha` to `scripts/release-readback.mjs:resolveReleaseForPublish` before checkout/build/publish. The wrapper rejects unsupported events and missing/malformed event SHAs before reading, then requires the independently peeled tag commit to equal the event SHA. `scripts/check-bootstrap-workflow.mjs` pins the workflow wiring, rejection gate and step order. Readback and workflow-contract tests exercise the negative paths; existing optional `resolvePublishedRelease` callers are unchanged.
- Verification: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/npm-release.test.mjs test/release-readback.test.mjs test/check-bootstrap-workflow.test.mjs` passed **31/31**; `node scripts/check-bootstrap-workflow.mjs` passed all five contracts; `pnpm test` passed **179/179**; `node scripts/check-determinism.mjs` passed; `git diff --check` passed. The initial focused invocation before building failed with `ENOENT` for `dist`; building the clean worktree resolved that test prerequisite, and the full required sequence then passed.
- Offline runtime boundary: injected GitHub read-client fixtures accepted release and manual events at the exact tag SHA, rejected mismatched event SHA for both events, and rejected missing/malformed SHA before any read. No live GitHub/npm call, dispatch, registry fetch, signed attestation or publish was made. A manual rerun is **not** safe until a future npm version-exists/idempotency and unknown-outcome stop gate is implemented; R184-02b's independently verified registry provenance remains open.
- Rollback: revert child #2's workflow, readback helper, static checker, two associated test files, and this evidence only. No published package, Release, tag, metadata, template setting, payload input or secret changed.
- Chain: `feat/issue-184-integrator` → `feat/issue-184-release` (child #1 at `97ee3d3`) → **📍 `feat/issue-184-release-identity` / R184-02b1** → R184-02b attestation and subsequent release gates (not complete). Child #2 is offline evidence, not #184 completion or publication authorization.

### Child #3 local work-unit evidence (R184-02b2 only)

- Boundary: `fix/issue-184-toolchain` starts from child #2 `85d61aa`; `.github/workflows/npm-release.yml` replaces only the Corepack toolchain activation with exact npm-installed pnpm and explicit Node/pnpm version checks before build. `scripts/check-bootstrap-workflow.mjs` and `test/check-bootstrap-workflow.test.mjs` pin the toolchain step and reject broken, floating, comment-only or out-of-order alternatives. The event-SHA guard and single-publish logic remain unchanged.
- Verification: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/check-bootstrap-workflow.test.mjs test/npm-release.test.mjs` passed **22/22**; `node scripts/check-bootstrap-workflow.mjs` passed all five contracts; `pnpm test` passed **180/180**; `node scripts/check-determinism.mjs` passed; `git diff --check` passed. Local runtime is Node `v26.9.0`: these offline results do **not** prove the hosted Node `20.19.0` path.
- Runtime boundary: static workflow fixtures exercised the pre-build toolchain contract and negative cases without Corepack installation, global npm installation, credentials, publish, live npm readback or hosted execution. Rollback: revert child #3's workflow, checker, negative test, and this passive ledger evidence only; no tag, npm artifact, source identity, repository metadata or Template mode changed.
- Chain: `feat/issue-184-integrator` → `feat/issue-184-release` → `feat/issue-184-release-identity` (child #2 at `85d61aa`) → **📍 `fix/issue-184-toolchain` / R184-02b2** → R184-02b independent attestation and subsequent gates (not complete). Independent #181 hosted Node 20 green, independent npm provenance, version idempotency, and tag/history gates still block publication.

### Child #4 local work-unit evidence (R184-02b3 only; PARTIAL)

- Boundary: `fix/issue-184-publish-idempotency` starts from child #3 `05a0c34`. Existing `scripts/npm-release.mjs` exports a read-only exact-version probe using only the fixed npm public registry origin and scoped URL, a 5-second maximum total deadline, a 64-KiB limit per successful response, and an injected transport. It does not call fetch itself or receive authentication. The exact endpoint and package document must agree before returning `present`; only matching 404/no-version observations yield `absent`. Any disagreement, wrong target, malformed body, failed read or timeout yields `unknown` without printing a transport error. Existing `.github/workflows/npm-release.yml` has no probe or skip wiring and still publishes unconditionally when run.
- Focused offline tests: `node --test test/npm-release.test.mjs` passed **4/4**, exercising two absent cases, agreed present, appearance/disappearance races, identity mismatches, malformed data, HTTP failures, network exception, timeout and oversized response with fake transport; no public registry request was made. `pnpm build` passed; `pnpm typecheck` passed; `node scripts/npm-release.mjs --self-check` passed; `pnpm test` passed **182/182**; `node scripts/check-bootstrap-workflow.mjs` passed all five workflow contracts; `git diff --check` passed. Local Node `v26.9.0` tests do not prove hosted Node `20.19.0` execution. The first `pnpm build` invocation reconciled three development dependencies from the existing local content-addressable store without downloading packages; no explicit install or remote registry request was performed.
- Runtime boundary: N/A for live npm because the probe is not wired into release execution; offline fake HTTP responses exercise the read-only contract. No npm publish, GitHub write, hosted dispatch, token access, tag/Release change or package existence claim. Rollback: revert only this child’s probe, offline tests and ledger additions; child #3 event-SHA/toolchain safeguards and the current release workflow remain intact.
- Chain: `feat/issue-184-integrator` → `feat/issue-184-release` → `feat/issue-184-release-identity` → `fix/issue-184-toolchain` (child #3 at `05a0c34`) → **📍 `fix/issue-184-publish-idempotency` / R184-02b3** → future durable exclusive attempt claim plus independent attestation/source identity and remaining release gates. A 404-only gate cannot make an unknown publish attempt idempotent; neither `absent` nor `present` is authorization to publish or skip. #181 typed-module reconciliation and #183 coordinates remain pending.

### Child #5 local work-unit evidence (R184-02b4 only; PARTIAL)

- Boundary: `feat/issue-184-publish-claim` starts at child #4 `96d31f1`; only `scripts/release-readback.mjs`, `test/release-readback.test.mjs` and this task ledger change. The injected client accepts a fresh Release ID supplied by a future verified caller; no live new Release exists in this offline unit. The simple alphanumeric asset name avoids GitHub filename renaming. The client posts at most once to that ID and requires 201, exact uploaded ID/name/size in a unique scoped list result, and byte-for-byte GET content. The payload is limited to 1 KiB, list to 64 KiB, and total time to five seconds; incomplete pagination, 422, 502/starter, malformed responses and any unknown outcome stop without retry or asset deletion. GitHub [release asset REST documentation](https://docs.github.com/en/rest/releases/assets) specifies 200 or 302 binary GET, duplicate-name 422 and possible starter after 502; fake HTTP tests establish the local contract, not hosted behavior.
- Runtime boundary: offline fake HTTP only. No authenticated/live GitHub upload or deletion, npm fetch/publish, workflow dispatch, tag, Release or secret access. A successful claim is **not** a rerun permit; same-attempt recovery may only read/reconcile exact-version npm evidence later. `.github/workflows/npm-release.yml` remains unchanged and still publishes unconditionally when triggered. This foundation does not establish first-publish exclusivity until the caller verifies a newly created release ID and serializes attempts under existing per-tag Actions concurrency, then integrates the claim with registry probe and independent attestation and tests every publish/skip/unknown path. Direct 200 retains exact bounded byte readback; a 302 requires an absolute HTTPS Location at the exact `release-assets.githubusercontent.com` origin ([GitHub runner domain reference](https://docs.github.com/en/actions/reference/runners/self-hosted-runners#accessible-domains-by-function)), then one credential-free injected download transport with manual redirects, the same deadline/size bound, and exact bytes. Missing or other origins, absent separate transport, further redirects, mismatches and timeouts block. This is offline fixture coverage, not a guarantee GitHub will always choose that CDN origin; unrecognized hosts require explicit review, not automatic following.
- Chain: `feat/issue-184-integrator` → `feat/issue-184-release` → `feat/issue-184-release-identity` → `fix/issue-184-toolchain` → `fix/issue-184-publish-idempotency` (child #4 at `96d31f1`) → **📍 `feat/issue-184-publish-claim` / R184-02b4** → integrated publication gate and R184-02b independent attestation (not complete). Rollback: revert only this child's claim helper, fake tests and ledger entries; no previous release guards, published bytes or historical objects change.
- Initial verification at `036b172`: `pnpm build` passed (three development dependencies hard-linked from the existing store, zero downloaded); `pnpm typecheck` passed; `node --test test/release-readback.test.mjs` passed **15/15**; `node scripts/check-bootstrap-workflow.mjs` passed all five contracts; `pnpm test` passed **187/187**; `git diff --check` passed. The initial fake scenarios covered 201 plus scoped list and exact content, 422, 502, starter, lost response, actual bounded timeout, malformed/oversized bytes, network failure, duplicate/wrong asset, full page and rejected binary redirect without a second write. That unconditional redirect rejection was a candidate-caused defect corrected in this child; the original authored diff was **203 additions + 2 deletions = 205 lines** against child #4. Local tests do not establish hosted API behavior or safe npm publication.
- Correction verification (offline): `pnpm build` passed; `pnpm typecheck` passed; `node --test test/release-readback.test.mjs` passed **18/18**; `node scripts/check-bootstrap-workflow.mjs` passed all five contracts; `pnpm test` passed **190/190**; `git diff --check` passed. Fake HTTP covers a valid 302, separate transport without forwarded Bearer credentials, unapproved/missing Location, second redirect, mismatched/oversized bytes and shared deadline; no live asset call or publish was made. Child #5 total authored diff against `96d31f1` is **292 changed lines**, under the 400-line advisory. The rollback boundary remains only this child's helper, test and task ledger changes.

### Child #6 local work-unit evidence (R184-02b5 only; PARTIAL)

- Boundary: `feat/issue-184-provenance-proof` begins at child #5 `b779be6`; `scripts/npm-provenance-policy.mjs` is a pure post-publication policy comparison, with offline fixtures in `test/npm-provenance-policy.test.mjs`. It is not exported to the release workflow, does not call npm or fetch the registry, and cannot make `scripts/npm-release.mjs:verify-registry`'s copied `expected.release.sha` independent. The current publish workflow remains unchanged and unconditionally publishes when triggered. `archetype-ownership.json` registers the script as release-only `removed`; because the ownership inventory is in the payload, `package/payload-manifest.json` was derived with `node scripts/build-payload.mjs --write-lock`. The validator and its tests are not downstream payload files.
- Evidence basis: locally installed npm CLI **11.19.1** `lib/utils/verify-signatures.js` yields `invalid`, `missing`, `verified[]` with `name/version/location/registry/attestationBundles`; its bundled `pacote/lib/registry.js` verifies each Sigstore bundle and the signed subject against manifest integrity but does not supply raw downloaded tarball bytes. Its bundled `libnpmpublish/lib/provenance.js` emits SLSA v1 `https://slsa-framework.github.io/github-actions-buildtypes/workflow/v1`, workflow repository/ref/path and `git+<repository>@<ref>` plus `digest.gitCommit`. Read-only local source inspection, not a real audit run or proof that a published package exists. The policy requires the singleton certificate SAN URI to equal the explicit workflow identity; a fixture self-signed certificate does **not** establish trusted signer evidence.
- Initial `pnpm build` failed on the expected payload-lock mismatch after registering the new path; deriving the lock resolved it without changing payload file membership. Then `pnpm build` passed, `pnpm typecheck` passed, `node --test test/npm-provenance-policy.test.mjs test/npm-release.test.mjs test/release-readback.test.mjs` passed **24/24**, `node scripts/check-bootstrap-workflow.mjs` passed all five contracts, `node scripts/npm-release.mjs --self-check` passed, `pnpm test` passed **192/192**, `pnpm test:package-consumer` passed **4/4**, `node scripts/check-determinism.mjs` passed, and `git diff --check` passed. Offline negative cases cover absent/duplicate audit entries or bundles, missing/invalid claims, wrong registry/version/location, tarball bytes/digests, signed subject/source/ref/build/workflow/builder, certificate signer SAN and unsupported npm version. No npm audit run, live attestation, registry download or hosted Node 20 run was performed: runtime release harness **N/A** until the production adapter and first publication exist.
- Child-only authored diff measured against `b779be6`: **207 changed lines** (81-line policy, 108-line test, 1 ownership line, 6 derived lock lines and 11 ledger lines); below the 400-line review budget without trimming tests. This is an offline checkpoint, not a reviewed or published release.
- Remaining gate: future code must install the exact public registry version, pin and successfully execute the actual npm CLI, verify its JSON and installed dependency provenance and independently download the raw registry tarball before passing those bytes to this policy. Review real output shape and Sigstore identity for the first publication. A policy match from caller-supplied fixtures is **not** verified provenance; an audit cannot attest an unpublished first package. The first publish cannot be retrospectively gated by this post-publication check, and the exclusive publish/skip/unknown-outcome flow, #181/#183 reconciliation and all release gates stay pending. Rollback: revert only this child's policy module, tests, ownership entry, derived payload lock and this ledger evidence; child #5 and historical remote objects remain unchanged.
- **R184-02b5 correction checkpoint:** independent validation found the original policy required only `verificationMaterial.x509CertificateChain.certificates[0].rawBytes` (Sigstore v0.2). Local npm 11.19.1 audit passes the verified bundle through unchanged, and its Sigstore builder can emit v0.3 `verificationMaterial.certificate.rawBytes`; the old policy would reject that legitimate shape. The policy now accepts exactly one valid v0.3 certificate or singleton v0.2 chain, rejecting both forms together, absent/empty/malformed bytes, multiple chain certificates, public-key material and incorrect SAN. Offline self-signed fixtures exercise both positive formats and their negative cases; they do **not** verify a signature or trust the certificate.
- Correction verification: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/npm-provenance-policy.test.mjs test/npm-release.test.mjs test/release-readback.test.mjs` passed **26/26**; `node scripts/check-bootstrap-workflow.mjs` passed all five contracts; `node scripts/npm-release.mjs --self-check` passed; `pnpm test` passed **194/194**; `pnpm test:package-consumer` passed **4/4**; `node scripts/check-determinism.mjs` passed; `git diff --check` passed. No npm audit, live registry fetch, hosted Node 20 run or release workflow execution occurred: production release harness **N/A**, since no production adapter or first published package exists.
- Correction rollback: revert only the certificate-shape policy, its offline fixtures and this checkpoint; retain child #6's original policy, inventory, lock and earlier evidence. The production adapter, independent registry provenance, first-publish boundary, exclusive publish/skip gate and #181/#183 integration remain unresolved; this checkpoint does not authorize publication.

### R184-02b6 local adapter checkpoint (post-publication only; PARTIAL)

- Boundary: `scripts/npm-provenance-audit.mjs` offers a production entrypoint that accepts expected release identity, never caller-supplied npm CLI paths, audit JSON, subprocess output or HTTP evidence. It resolves npm beside the running Node installation; a separate fixture entrypoint exercises the same guarded sequence offline. It checks the installed CLI package metadata and actual `--version` output for 11.19.1, executes isolated `npm install --ignore-scripts` with a fresh cache and exact public-registry dependency, checks actual installed package and exact lock entries, then independently obtains the exact-version registry metadata and raw tarball using manual redirects, a fixed origin/path, an 8-second request timeout and byte caps. Registry SHA512 integrity must match the lock and downloaded bytes; the pure policy compares those bytes to the expected local SHA256 and the signed statement. Finally `audit signatures --json --include-attestations` must exit successfully with one exact verified dependency before policy evaluation. npm credentials/config and lifecycle scripts are not inherited. Provisioning the trusted Node/npm installation remains an operator responsibility; reading npm's adjacent package version is not cryptographic proof of that installation.
- The locally installed npm 11.19.1 `lib/commands/audit.js` loads the actual dependency tree; `lib/utils/verify-signatures.js` emits `invalid`, `missing`, and `verified[]` only with JSON/attestation flags. `pacote/lib/registry.js` checks attestation subject against registry integrity and verifies the Sigstore bundle. These observations are from local installed source, not a successful real audit. Fixture self-signed certificates and fake `verified[]` never constitute signature verification.
- Verification (after final source edits): `pnpm build` passed; `pnpm typecheck` passed; `node --test test/npm-provenance-policy.test.mjs test/npm-provenance-audit.test.mjs` passed 8/8; `node scripts/check-bootstrap-workflow.mjs` passed five contracts; `pnpm test` passed 198/198; `pnpm test:package-consumer` passed 4/4; `node scripts/check-determinism.mjs` passed; `git diff --check` passed. Focused offline runtime boundary: injected subprocess/HTTP cases exercised exit/version failure, installed and lock identity, registry integrity, origin/redirect/size/transport failure, malformed and ambiguous audit claims, and a production entrypoint that ignores forged injection fields. No real npm audit, signature check, network request, hosted Node 20 test or publication occurred.
- This is NOT wired into `.github/workflows/npm-release.yml`. There is no public registry package yet; first publication cannot be gated by a post-publication audit of an absent attestation. The existing workflow still publishes unconditionally on its trigger, and exclusive publish/skip/unknown-outcome handling, #181/#183 integration and live first-publish review remain blocked. Do not infer an npm registry existence claim from fixture success. Ownership registers only the release-only adapter as `removed`; the ownership inventory change required deriving `package/payload-manifest.json` via `node scripts/build-payload.mjs --write-lock` without adding the adapter to the downstream payload. Rollback: remove only `scripts/npm-provenance-audit.mjs`, `test/npm-provenance-audit.test.mjs`, its ownership entry and this checkpoint, then derive the previous payload lock; retain the child #6 policy, workflow and all historical release objects unchanged.
- Independent offline validation read the local npm 11.19.1 audit/pacote source, reran the adapter fixture (4/4), and checked ownership/payload integrity; no candidate-caused severe defect was found in this bounded scope. A separate parent spot check also passed 4/4. Work-unit commit `f3dcf2f8742950699e2d365fcbfe087d3cc1b0a3` on `feat/issue-184-provenance-audit` follows parent PR #203 head `02294e92df4c9403b357424fbb365496667b3c2f`; **297 additions + 3 deletions = 300 authored lines**. RDD remains globally off; native committed-range assessment was `medium` (`configuration_change`), `under_budget`. No native review receipt or hosted npm proof is claimed. Next: deliver this child as a draft PR targeting `feat/issue-184-provenance-proof`, then tackle exclusive first-publish/skip/unknown-outcome wiring in a separate unit. No release action is authorized by this checkpoint.

### R184-02b7 abandoned precommit trial

- The uncommitted 218-line candidate on `feat/issue-184-publish-decision` at `5dd88d4` always returned blocked, was called only from tests, and left `npm publish` reachable on both triggers. Its claim read had a time-of-check/time-of-use gap and a timeout could leave reads running. Review rejected it; the module/test, ownership entry and derived lock change were removed without committing or touching earlier checkpoints. Its previous green fixture tests were not a delivered publish gate.

### R184-02b8 temporary publication stop (scope before implementation)

- Boundary: modify only `.github/workflows/npm-release.yml`, its static checker, its focused mutation tests and this task record. The publish step must fail unconditionally as its first shell command before token checks or `npm publish`; do not depend on secrets, environment flags, approvals, tags or network operations. Both `release.published` and `workflow_dispatch` use the same step. Check exact placement and shell termination; reject comment/decoy guards, reordered commands and `continue-on-error` or `if: always()` bypasses. No tag, Release, npm publication, dispatch or Template-mode change.
- Checklist: [x] stop is the first publish-step shell command; [x] checker pins the complete publish step, forbids bypass/alternate shell configuration and retains the one-publish check; [x] offline mutations and a local shell harness prove the stop; [x] all requested sequential verification passed; [x] rollback recorded. The stop exits 1 before `test -n "$NODE_AUTH_TOKEN"` or `npm publish` even when the token is absent.
- Verification at this uncommitted checkpoint: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/check-bootstrap-workflow.test.mjs test/npm-release.test.mjs test/release-readback.test.mjs` **44/44**; `node scripts/check-bootstrap-workflow.mjs` **5/5** contracts; `pnpm test` **200/200**; `pnpm test:package-consumer` **4/4**; `node scripts/check-determinism.mjs` passed; `git diff --check` passed. The full and consumer commands each ran their own nested build sequentially. No failures or reruns in the required sequence; a preliminary checker and focused workflow run also passed (22/22).
- Offline runtime boundary: the test extracted the actual publish-step shell and invoked Bash four times (both event names, absent or dummy token). Every invocation exited 1 with the stop message; a fake `npm` executable remained uncalled. Static mutation fixtures rejected deleted, relocated, commented, conditional, neutralized or reordered stops and job/step `continue-on-error`, `if: always()` and alternate shell defaults. No hosted Actions run, registry installation, signed audit, claim, npm publish or GitHub write was performed.
- Rollback: restore only this unit's workflow/checker/tests and R184-02b8 ledger lines, **only after** a separately reviewed and tested exclusive publish/skip/unknown gate replaces the stop. Restoring the old unguarded publish step alone is unsafe. Until a separately authorized merge carries this guard to `main` (currently `d6b0f68`), its release workflow remains unguarded. The first-publish npm attestation cannot exist before publication.
- Independent high-risk validation of this exact candidate found no candidate-caused severe bypass in the current workflow: focused checker tests **22/22**, five static contracts and `git diff --check` passed; both event paths use one stopped publish step. This is textual/static plus offline Bash evidence, not a hosted release. Work-unit commit `32a60a87068c886b9ccd2526fcc900d6aea8ad86` contains 88 additions + 1 deletion = **89 authored lines**; RDD is globally off and native committed-range risk was `high` for `shell_source` (no native receipt). The abandoned R184-02b7 files and ownership/payload changes are absent. Next: deliver this child against draft #213, then explicitly authorize a guarded main integration if immediate protection of the currently unguarded default workflow is desired; do not publish or dispatch in the meantime.

### #183 main reconciliation checkpoint (integrator)

- Merged local `refs/remotes/authorized/main-for-184` at exactly `5fe1a85ba31caa091c4d94c9baca22b6b9cbc381` into `feat/issue-184-integrator` (HEAD `392a479b3b0d57b903d03686864a7f03ed8e850c`) with `--no-commit --no-ff`. The sole content conflict was the derived `package/payload-manifest.json`; `node scripts/build-payload.mjs --write-lock` regenerated it from the combined #183 files and #184 ownership inventory. No parent lock was selected. The auto-merged workflow checker and tests retain both #183 journey-coordinate/commit-caption assertions and #184's exact stopped publish-step contract; the shared workflow still stops before its token check and `npm publish` for both triggers.
- Sequential offline verification after reconciliation: `node start.mjs` reported SELF mode; `pnpm build` and `pnpm typecheck` passed; focused workflow/release/provenance tests passed **55/55**; `pnpm test` passed **207/207**; `pnpm test:package-consumer` passed **4/4**; `node scripts/check-bootstrap-workflow.mjs` passed five contracts; `node scripts/check-determinism.mjs` passed; staged and unstaged `git diff --check` passed. No hosted run, registry access, release action, commit or push was performed. The nested builds in the full and consumer test commands ran sequentially.
- Reconciliation-only authored changes beyond the expected #183 merge: regenerated payload lock, this checkpoint and a correction to `MAINTAINERS.md` describing the actual temporary stop. Rollback before a merge commit: `git merge --abort` restores the pre-merge feature tip, but first preserve this uncommitted reconciliation-only documentation and lock resolution if needed. To undo the publish stop after integration, replace it only with a separately reviewed exclusive publish/skip/unknown gate; never remove it alone. #184 remains open and unpublished; this stop is **feature-only** and must not be described as guarding `main` until separately merged there.
- Merge commit `86ad90363c11bfc4784015be8ac630b67f688dc3` has first parent #184 integrator `392a479b3b0d57b903d03686864a7f03ed8e850c` and second parent verified #183 main `5fe1a85ba31caa091c4d94c9baca22b6b9cbc381`; the commit tree matched the fully staged and tested merged tree and the worktree was clean after commit. Independent read-only high-risk validation checked the exact combined workflow, full 62-file generated payload identity, focused 29/29, static five contracts and diff check without finding a candidate-caused severe issue. Native committed-range assessment was `high` for inherited #183 shell workflow; RDD is globally off, so the independent check is not a native review receipt. This commit is branch reconciliation, not a 405-line newly authored #184 work unit; inherited #183 changes account for the accumulated diff. Next: push only the integrator branch and read back PR #213 head/checks, keeping the remotely disabled npm-release workflow disabled and #184 open.

### R184-05a scoped journey metadata continuation (local offline checkpoint)

- Starting point: clean integrator `d5f67abbcf00f800bd22b3f36f78d765414a5e42` on `feat/issue-184-integrator`, with local draft #213 association recorded above; continuation branch `fix/issue-184-scoped-journey-spec` preserves the integrator. No live PR readback was authorized. The existing metadata heredoc splits on every `@`, so the leading scope separator produces an empty name. This is inherited, not a #183 reconciliation change.
- Boundary: only `.github/workflows/real-agent-journey.yml`, its existing static checker and negative tests, and this ledger. Keep the expected exact package name, semver, source SHA, metadata equality and generated source identity; reject malicious package specs. No npm-release edits, release/tag/npm writes, hosted dispatch, #181 paths or unrelated journey stages.
- Implementation: the metadata heredoc takes the version only after the exact scoped package prefix, rejects non-exact semver and extra separators, compares both name and version with npm metadata, and writes the unchanged source-SHA/package-identity envelope only after all guards pass. The existing checker pins the actual step between `npm view` and `npx apply`; offline mutations reject the old split, weakened name/version checks and altered package-spec wiring.
- Sequential verification on the final candidate: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/check-bootstrap-workflow.test.mjs test/real-agent-journey-report.test.mjs test/package-consumer-verify.test.mjs` passed **41/41**; `pnpm test` passed **209/209**; `pnpm test:package-consumer` passed **4/4**; `node scripts/check-bootstrap-workflow.mjs` passed **5/5** contracts; `node scripts/check-determinism.mjs` passed. `scripts/typed-runtime/check-real-agent-workflow.js` is absent in this checkout: **N/A**, not silently skipped. The full and consumer commands include nested builds, executed sequentially. Both staged and unstaged `git diff --check` passed after staging only the four R184-05a paths; no other paths changed. Authored scope: 86 additions + 1 deletion = **87 lines**, below the 400-line advisory without shrinking tests.
- Offline runtime harness: the focused test extracts and runs the actual Node metadata heredoc with an exact scoped release and a prerelease/build version, checks the unchanged full SHA/name/version/spec output, and rejects unscoped or wrong-scope names, tags, incomplete/invalid semver, extra `@`, wrong metadata name/version and altered declared package name. Rejected specs write no source-identity file. The harness uses temporary JSON fixtures only; no `npm view`, `npx`, hosted Actions, registry request or secret access was performed.
- Independent read-only high-risk validation of the staged candidate found no candidate-caused severe defect: `node --test test/check-bootstrap-workflow.test.mjs` passed **27/27**, `node scripts/check-bootstrap-workflow.mjs` passed **5/5**, and staged/unstaged `git diff --check` passed. The parent also spot-checked the checker. A separate attempt to isolate network with `unshare --net` failed with `Operation not permitted`; no network-isolated test run occurred. These results are separate from the writer's sequential 41/41, 209/209 and 4/4 evidence above, and are not hosted or published-package proof.
- Work-unit commit: `562dee4e911791f8c449cbca6a7319f646321d2b` (`fix(journey): validate exact scoped package metadata (#184)`).
- Rollback: revert only R184-05a's journey heredoc, static checker, tests and continuation ledger lines; leave the integrator, stopped publish workflow, historical baseline and approved history policy intact. The hosted real-agent journey, public package/version existence, independently verified registry provenance, exact first-publish gate and final release acceptance remain **unresolved**; offline checks are not publication evidence. No tag, Release, npm publication, dispatch, issue close, Template-mode change or remote mutation was made.

### R184-05b journey YAML syntax continuation (definition checkpoint)

- Starting point: clean `fix/issue-184-scoped-journey-spec` at `33efb8cb1ad2034d99af67f29a3fb045e1b44821`, draft #217; new local child `fix/issue-184-journey-yaml`. Read-only triage attributed zero-job runs 36020337249, 36030653049 and 36030692600 to the two inherited `run: |` indentation defects, also observed on `main` at `d6b0f68`. That triage is not hosted journey execution evidence.
- Scope: remove only one excess space from each `branch=$(...)` line (agent/provision-readback and assertion metadata). The existing Node checker now inspects every `run: |` block in the entire journey file, infers the first content line's YAML indentation, and rejects later content with less indentation. Offline mutations of either corrected line must fail the checker; the existing #217 metadata heredoc and all other workflow semantics remain unchanged. The npm release publish stop remains in place. No registry, release, tag, dispatch, remote credential, commit, push, PR or Template-mode change.
- Complete-workflow parser proof (local PyYAML 6.0.3, not a packaged test dependency): parsing `HEAD:.github/workflows/real-agent-journey.yml` raised `ParserError` at line 175; parsing the corrected complete workflow returned six jobs (`prepare`, `provision`, `agent`, `assert`, `cleanup`, `report`). Independently reintroducing the extra space at each corrected line in memory raised `ParserError` at line 175 and line 288 respectively. The committed regression uses the dependency-free Node static indentation guard, not PyYAML: it protects this failure class but is not a general YAML parser or hosted Actions validation.
- Verification after the final source change, sequentially: `pnpm build` passed; `pnpm typecheck` passed; `node --test test/check-bootstrap-workflow.test.mjs test/real-agent-journey-report.test.mjs` passed **38/38**; `pnpm test` passed **210/210** (nested build); `pnpm test:package-consumer` passed **4/4** (nested build); `node scripts/check-bootstrap-workflow.mjs` passed **5/5** workflow contracts; `node scripts/check-determinism.mjs` reported `determinism and Python-removal audit OK`. An initial focused run failed **23/38**, because the first guard erroneously required all block scalars to start at exactly two spaces beyond `run: |`; corrected to infer legal YAML block indentation before restarting the complete verification sequence. No failed result is claimed green.
- Offline runtime boundary: Node mutation tests run the checker against a full temporary workflow for both inherited extra-space regressions, and the existing scoped-package test executes its metadata heredoc against positive and negative fixtures. Parser proof loads the entire original and corrected YAML locally without dispatch, secrets or remote access. The six parsed jobs have not run on GitHub. Rollback boundary: revert only this child's two workflow spaces, the Node indentation guard and its test, plus the R184-05b ledger entries; retain #217's scoped parser, prior ledger history and the feature-only npm publication stop. Hosted journey, public package/version existence, independently verified registry provenance, exclusive first-publish gate and #184 release acceptance remain unresolved.
- Local review scope: only the journey YAML, existing checker, its test and this ledger are staged; `git diff --cached --check` and `git diff --check` both passed, with no unstaged file changes. The staged candidate is 45 additions + 2 deletions = **47 authored changed lines**, below the ~400-line advisory. No commit was created.
- Independent read-only validation reproduced the parent PyYAML `ParserError` at lines 175 and 288 by restoring each excess space separately, parsed the corrected workflow with six jobs, passed focused workflow tests **28/28** and five static contracts, and found no candidate-caused severe defect. A parent spot check also passed. This is offline evidence only, not a hosted journey or publication check.
- Work-unit commit: `098323eca863ee3210e2db915bc4ca50a9e71e1d` (`fix(journey): restore valid real-agent workflow YAML (#184)`).

### R184-05c PR #213 exact-main conflict reconciliation (definition checkpoint)

- Starting point: clean isolated `fix/issue-184-pr213-conflicts` at PR #213 head `2f4991824b84668283c28c9672157412866e7cc8`; merge input is the local commit `4b2b90bf4028fc86d5b401318965eccf1d682291`, not the older local `main` ref. Read-only public readback matched both remote SHAs and found release workflow `365030867` `disabled_manually` before local edits. Issue #184 and its release, provenance, first-publish, changelog, public-consumer and hosted-journey acceptance remain open.
- Boundary and route: only this fix worktree; merge without committing, resolve the two expected ownership/manifest conflicts, preserve both release-only provenance classifications and main's typed runtime and #177 retained ports, derive the payload lock from the resolved tree, and inspect the shared auto-merges for event SHA, pinned toolchain, registry launchers and the typed migration. A third conflict stops the task with merge state intact. No release, registry, remote workflow, credential, Template-mode or other-worktree action is in scope.
- Acceptance: source npm publish must stop unconditionally with `exit 1` before the token check or `npm publish` on both release and manual events; offline harnesses must not invoke fake npm. Run `pnpm build`, `pnpm typecheck`, the named existing focused release/provenance/workflow/consumer/creator/package tests, `pnpm test`, `pnpm test:creator`, `pnpm test:package-consumer`, `pnpm test:workflow-contract`, both static workflow and determinism checks, and both staged and unstaged diff checks sequentially where builds can overlap. Record omissions and exact failures; at most one scoped correction with regression for a deterministic integration failure. Do not claim public npm or hosted proof or mark #184 done.
- Rollback: before a merge commit, preserve this additive task evidence separately and `git merge --abort` to return to the PR head; restore the task evidence only if needed. A docs-only Conventional Commit on this fix branch is permitted solely if the dirty task document prevents the requested merge. Never remove the publication stop alone: replace it only under a separately reviewed exclusive first-publish gate. No release history or published bytes are part of this local rollback.
- Reconciliation result (local, uncommitted): `HEAD=2f4991824b84668283c28c9672157412866e7cc8`, `MERGE_HEAD=4b2b90bf4028fc86d5b401318965eccf1d682291`. Exactly `archetype-ownership.json` and `package/payload-manifest.json` conflicted; both are resolved and staged. Ownership retains the release-only npm provenance policy/audit paths as `removed`, omits retired `scripts/resource-proof-cleanup.mjs`, and keeps the typed runtime and #177 inherited task ports. `node scripts/build-payload.mjs --write-lock` derived **68** declared payload files with matching current file bytes, modes, SHA-256 entries and `payload_digest=sha256:81cd848e1d9878ec1a784823a3fc13717c55d4b5a9bb57494454ad9873625db6`. The additive ODD evidence remains unstaged; the dirty document did not prevent the merge, so no docs-only commit was made.
- Auto-merge audit: `MAINTAINERS.md` retains feature-only stop wording and updates the typed inherited labels entrypoint; `scripts/check-bootstrap-workflow.mjs` and `test/check-bootstrap-workflow.test.mjs` retain the full event-SHA/pinned npm toolchain/stopped publish-step checks while adding typed bootstrap imports and the Node 20 PR workflow contract; `scripts/package-consumer-verify.mjs` imports typed artifact identity and retains both registry `apply`/`verify`/noop launchers. The source workflow still has one shared publish step whose first shell commands print the block and `exit 1`, before `NODE_AUTH_TOKEN` check and `npm publish` for both events. No change to that stop was required.
- Sequential offline verification after final source/lock changes: `pnpm build` passed (three packages reused from local pnpm store, zero downloaded); `pnpm typecheck` passed; the nine existing requested focused test files passed **110/110** (no omitted paths); `pnpm test` passed **253/253**; `pnpm test:creator` passed **23/23**; `pnpm test:package-consumer` passed **4/4**; `pnpm test:workflow-contract` passed **29/29**; `node scripts/check-bootstrap-workflow.mjs` passed **6/6** static workflow contracts; `node scripts/check-determinism.mjs` passed. No test failed and no corrective implementation was needed. Each build-invoking command ran to completion before the next began.
- Offline runtime boundary: the focused and workflow-contract test suites extracted the *actual* source publish-step shell and ran four Bash cases (`release` and `workflow_dispatch`, each with absent and dummy token); every case exited 1 with the block message before token check and left the fake npm marker absent. Fixture checks and packaged local consumer/creator tests are not public npm provenance, hosted Actions/Node 20, or exact published-version consumer evidence. No live registry probe, authenticated request, Release, tag, dispatch, push, merge commit or Template-mode change occurred.
- Review boundary: PR #213's previously reported 1,470 additions + 25 deletions = **1,495** changed lines at the exact head; this is already over 400 with **no size exception**. The locally reconciled diff relative to exact main is **1,478 additions + 25 deletions = 1,503** changed lines before this additive task evidence, also over budget; no PR edit or slice was authorized. The prior child PRs remain the original review slices, not a claim that this integrator is within budget. Parent independent validation and spot check are pending.
- Independent pre-commit validation found no severe defect in the local conflict resolution: the 78-path staged identity remained stable, with no unstaged or unmerged entries; all 68 payload bytes, modes and hashes matched the derived lock above, and both parents' retained issue-specific paths survived. The actual source publish shell exited before token inspection and fake npm for all four release/manual and token-present/absent cases. Sequential build/typecheck, focused **110/110**, full **253/253**, creator **23/23**, consumer **4/4**, workflow **29/29**, static **6/6**, determinism and diff checks passed. The parent independently read the unconditional `exit 1` ahead of `NODE_AUTH_TOKEN` and `npm publish`, then reran focused release/workflow/consumer tests **37/37**. This is offline merge-tree evidence, not a released package, hosted verification, issue acceptance or review-size exception.

## Acceptance and staged rollback

- [ ] The complete before-history snapshot and last-moment comparison prove only the six named tags and five named Releases are present/unchanged at the authorized reset boundary; any drift stops for renewed owner decision. `v0.1.0` old commit `d9d3592…` is explicitly superseded only by the final verified main commit, never by a mutable branch-name inference.
- [ ] R184-02a's three offline defects have negative regressions (wrong workflow identity key, mismatched registry release SHA, and missing `apply` in both registry consumers); offline verification is not registry provenance. Before any publish, R184-02b implements and tests a fail-closed independent attestation/source-SHA check for the exact registry package, with the first-publish limitation recorded; the live package claim can be verified only after it exists. A SHA seeded from the expected local identity cannot satisfy this gate. Offline deterministic changelog, idempotent release creation/dry-run, semver/tag/source validation, annotated-tag peeling, negative npm/readback cases and workflow checks pass; full build/typecheck/tests, generated archetype and #181/#183 integration gates are green **before** publication. GitHub metadata wording remains unapproved until #183 owner review.
- [ ] The operator reads back exact scoped retirement, final `v0.1.0` tag and Release, one published `@eff3ct/agent-foundry@0.1.0` with provenance, package/payload/tarball/source identity and durable bounded evidence. Missing or uncertain write result is blocked, never treated as success or blindly repeated.
- [ ] Both exact-version package consumer paths and the hosted real-agent journey pass with independently verified source identity and cleanup; Template mode remains `true` throughout these checks. Final Template-mode change, if any, needs its separate owner authorization and readback.
- [ ] DoD, issue evidence and review are complete before closure; R184-02a is only an offline local checkpoint, not release completion, publication or final cutover.

Rollback by stage: **before history writes**, discard the local release changes without affecting refs or npm; **after any retirement**, freeze, compare the preserved snapshot with live state and have the owner plan an exact-object recovery (recreated releases cannot regain original IDs or publication timestamps); **after new tag/Release but before npm**, stop/remove only a verified new object by owner decision, never retry a publish; **after npm publication**, immutable package bytes cannot be rolled back by unpublish—retain evidence, keep Template mode enabled and roll forward with a separately approved new version after diagnosis. A failed hosted journey or unknown cleanup keeps cutover blocked, not green.
