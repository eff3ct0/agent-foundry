# Pi workspace instructions

This file is generated by the factory creator for the selected Pi provider.

- Work only in this repository unless the user explicitly requests otherwise.
- Treat `AGENT.md` and `docs/bindings.md` as the repository contracts.
- Keep credentials, API keys, and other secrets out of files and command output.
- The factory creator remains the source of truth for setup; do not invent provider configuration.

Runtime installation, authentication, model selection, and global configuration are managed separately by the user.
